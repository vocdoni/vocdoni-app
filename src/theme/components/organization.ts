import { defineRecipe } from '@chakra-ui/react'

export const OrganizationName = defineRecipe({
  base: {
    fontWeight: 'bold',
  },
})

export const OrganizationDescription = defineRecipe({
  base: {
    color: 'texts.subtle',
  },
})

export const OrganizationImage = defineRecipe({
  base: {},
})
