// Re-exported from the app source so there is a single source of truth shared
// by the runtime (src/app-env-build.ts) and the build/test tooling
// (vitest.config.ts).
// @ts-ignore
export { baseLanguages, resolveLanguagesSlice } from '../src/i18n/languages'
