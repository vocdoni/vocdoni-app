var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  ActionCancel: () => ActionCancel,
  ActionContinue: () => ActionContinue,
  ActionEnd: () => ActionEnd,
  ActionPause: () => ActionPause,
  ActionsContext: () => ActionsContext,
  ActionsProvider: () => ActionsProvider2,
  Balance: () => Balance,
  ClientProvider: () => ClientProvider2,
  ComponentsProvider: () => ComponentsProvider,
  ConfirmActionModal: () => ConfirmActionModal,
  ConfirmModal: () => ConfirmModal,
  ConfirmProvider: () => ConfirmProvider,
  Election: () => Election,
  ElectionActions: () => ElectionActions,
  ElectionContext: () => ElectionContext,
  ElectionDescription: () => ElectionDescription,
  ElectionHeader: () => ElectionHeader,
  ElectionProvider: () => ElectionProvider,
  ElectionQuestion: () => ElectionQuestion,
  ElectionQuestions: () => ElectionQuestions,
  ElectionQuestionsForm: () => ElectionQuestionsForm,
  ElectionResults: () => ElectionResults,
  ElectionSchedule: () => ElectionSchedule,
  ElectionStatusBadge: () => ElectionStatusBadge,
  ElectionTitle: () => ElectionTitle,
  Envelope: () => Envelope,
  HR: () => HR,
  OrganizationAvatar: () => OrganizationAvatar,
  OrganizationContext: () => OrganizationContext,
  OrganizationDescription: () => OrganizationDescription,
  OrganizationHeader: () => OrganizationHeader,
  OrganizationImage: () => OrganizationImage,
  OrganizationName: () => OrganizationName,
  OrganizationProvider: () => OrganizationProvider,
  Pagination: () => Pagination,
  PaginationProvider: () => PaginationProvider,
  QuestionChoice: () => QuestionChoice,
  QuestionTip: () => QuestionTip,
  QuestionsConfirmation: () => QuestionsConfirmation,
  QuestionsEmpty: () => QuestionsEmpty,
  QuestionsError: () => QuestionsError,
  QuestionsFormProvider: () => QuestionsFormProvider,
  QuestionsTypeBadge: () => QuestionsTypeBadge,
  SpreadsheetAccess: () => SpreadsheetAccess,
  VoteButton: () => VoteButton,
  VoteWeight: () => VoteWeight,
  Voted: () => Voted,
  composeComponents: () => composeComponents,
  defineComponent: () => defineComponent,
  enforceHexPrefix: () => enforceHexPrefix,
  environment: () => environment,
  errorToString: () => errorToString,
  fetchSignInfo: () => fetchSignInfo,
  getQuestionChoiceMeta: () => getQuestionChoiceMeta,
  hasExtendedChoiceMeta: () => hasExtendedChoiceMeta,
  normalizeText: () => normalizeText,
  queryKeys: () => queryKeys,
  reactComponentsDefaultLanguage: () => reactComponentsDefaultLanguage,
  reactComponentsNamespace: () => reactComponentsNamespace,
  reactComponentsResources: () => reactComponentsResources,
  results: () => results,
  useActions: () => useActions,
  useClient: () => useClient,
  useComponents: () => useComponents,
  useConfirm: () => useConfirm,
  useElection: () => useElection,
  useLocalize: () => useLocalize,
  useOrganization: () => useOrganization,
  usePagination: () => usePagination,
  useQuestionsForm: () => useQuestionsForm,
  useReactComponentsLocalize: () => useReactComponentsLocalize,
  vote: () => vote,
  walletFromRow: () => walletFromRow
});
module.exports = __toCommonJS(src_exports);

// src/components/context/useComponents.ts
var import_react2 = require("react");

// src/components/context/ComponentsProvider.tsx
var import_react = require("react");

// src/components/shared/HR.tsx
var import_jsx_runtime = require("react/jsx-runtime");
var HR = (props) => /* @__PURE__ */ (0, import_jsx_runtime.jsx)("hr", { ...props });

// src/components/shared/ipfs.ts
var linkifyIpfs = (link, gateway = "https://infura-ipfs.io/ipfs/") => {
  if (typeof link === "undefined") return;
  if (!link.startsWith("ipfs")) return link;
  const matches = link.match(/(?:ipfs:\/\/)?(.*)/);
  if (!matches) return link;
  const [, pin] = matches;
  return gateway + pin;
};

// src/components/context/default-components.tsx
var import_jsx_runtime2 = require("react/jsx-runtime");
var ActionButton = ({ label, loading, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("button", { ...props, children: [
  loading ? "..." : null,
  label
] });
var TrustedHtml = ({ html, ...rest }) => {
  if (!html) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...rest, dangerouslySetInnerHTML: { __html: html } });
};
var defaultComponents = {
  HR,
  ElectionTitle: ({ title, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("h1", { ...props, children: title }),
  ElectionDescription: ({ description, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(TrustedHtml, { html: description }) }),
  ElectionSchedule: ({ text, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("p", { ...props, children: text }),
  ElectionStatusBadge: ({ label, tone, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { ...props, "data-tone": tone, children: label }),
  ElectionHeader: ({ src, alt, ...props }) => src ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("img", { ...props, alt, src: linkifyIpfs(src) }) : null,
  ElectionQuestions: ({ form, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, children: form }),
  ElectionQuestion: ({
    title,
    description,
    fields,
    tip,
    hasExtendedChoices,
    selectionMode,
    layout,
    invalid,
    ...props
  }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(
    "section",
    {
      ...props,
      "data-has-extended-choices": hasExtendedChoices,
      "data-selection-mode": selectionMode,
      "data-layout": layout,
      "data-invalid": invalid ? "true" : void 0,
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("label", { children: title }),
        /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(TrustedHtml, { html: description }),
        fields,
        tip
      ]
    }
  ),
  QuestionChoice: ({
    choice,
    value,
    label,
    description,
    image,
    compact,
    canOpenImageModal,
    dataAttrs,
    presentation,
    selectionMode,
    selected,
    disabled,
    controlType,
    onSelect,
    ...props
  }) => {
    const id = `choice-${choice.value}`;
    const imageSrc = image?.thumbnail ?? image?.default;
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(
      "label",
      {
        ...props,
        "data-presentation": presentation,
        "data-selection-mode": selectionMode,
        "data-choice-card": dataAttrs?.["data-choice-card"],
        "data-layout": dataAttrs?.["data-layout"],
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
            "input",
            {
              id,
              value,
              type: controlType,
              checked: selected,
              disabled,
              onChange: (event) => onSelect(event.target.checked),
              "data-choice-control": dataAttrs?.["data-choice-control"]
            }
          ),
          imageSrc ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
            "img",
            {
              src: linkifyIpfs(imageSrc),
              alt: label,
              "data-choice-media": dataAttrs?.["data-choice-media"],
              "data-can-open-modal": canOpenImageModal ? "true" : void 0
            }
          ) : null,
          /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { "data-choice-body": dataAttrs?.["data-choice-body"], "data-compact": compact ? "" : void 0, children: label }),
          /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(TrustedHtml, { html: description })
        ]
      }
    );
  },
  QuestionsTypeBadge: ({ title, tooltip, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, title: tooltip, children: title }),
  QuestionTip: ({ text, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, children: text }),
  QuestionsEmpty: ({ text, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, children: text }),
  QuestionsError: ({ error, variant, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, "data-variant": variant, children: error }),
  QuestionsConfirmation: ({ election, answersView, onConfirm, onCancel, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("h3", { children: "Confirm" }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("p", { children: election.title.default }),
    answersView.map((item, idx) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("strong", { children: item.question }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { children: item.answers.join(", ") })
    ] }, idx)),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { onClick: onCancel, children: "Cancel" }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { onClick: onConfirm, children: "Confirm" })
  ] }),
  Voted: ({ title, description, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("strong", { children: title }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("p", { children: description })
  ] }),
  VoteButton: ({ label, loading, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { ...props, children: loading ? "..." : label }),
  VoteWeight: ({ label, weight, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { ...props, children: [
    label,
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { children: weight })
  ] }),
  ElectionResults: ({ secretText, questions, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { ...props, children: [
    secretText ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("p", { children: secretText }) : null,
    questions?.map((question) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("section", { children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("h3", { children: question.title }),
      question.choices.map((choice) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("strong", { children: choice.title }),
        " ",
        choice.votes,
        " (",
        choice.percent,
        ")",
        /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(TrustedHtml, { html: choice.description }),
        choice.image ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("img", { src: linkifyIpfs(choice.image), alt: choice.title }) : null
      ] }, choice.title))
    ] }, question.title))
  ] }),
  SpreadsheetAccess: ({
    connected,
    loading,
    formError,
    title,
    open,
    onOpen,
    onClose,
    onLogout,
    onSubmit,
    fields,
    anonymousField,
    extraFields,
    ...props
  }) => {
    if (connected) {
      return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { onClick: onLogout, disabled: loading, children: "Logout" });
    }
    if (!open) {
      return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { onClick: onOpen, disabled: loading, children: "Identify" });
    }
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { ...props, children: [
      /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("h3", { children: title }),
      /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)(
        "form",
        {
          onSubmit: (event) => {
            onSubmit(event);
          },
          children: [
            fields.map((field) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("label", { children: [
              field.label,
              /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("input", { ...field.inputProps, ...field.inputAttrs }),
              field.error ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("small", { children: field.error }) : null,
              field.description ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("small", { children: field.description }) : null
            ] }, field.id)),
            anonymousField ? /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("label", { children: [
              anonymousField.label,
              /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("input", { ...anonymousField.inputProps, ...anonymousField.inputAttrs }),
              anonymousField.error ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("small", { children: anonymousField.error }) : null,
              anonymousField.description ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("small", { children: anonymousField.description }) : null
            ] }) : null,
            formError ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("small", { children: formError }) : null,
            extraFields,
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { type: "button", onClick: onClose, children: "Close" }),
            /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { type: "submit", disabled: loading, children: "Identify" })
          ]
        }
      )
    ] });
  },
  ElectionActions: ({ actions, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, children: actions }),
  ActionContinue: ActionButton,
  ActionPause: ActionButton,
  ActionEnd: ActionButton,
  ActionCancel: ActionButton,
  ConfirmActionModal: ({ title, description, confirm, cancel, onConfirm, onCancel, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsxs)("div", { ...props, children: [
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("h3", { children: title }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("p", { children: description }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { onClick: onCancel, children: cancel }),
    /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { onClick: onConfirm, children: confirm })
  ] }),
  ConfirmShell: ({ isOpen, onClose, content, ...props }) => {
    if (!isOpen) return null;
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { className: "vocdoni-confirm-modal-overlay", role: "presentation", onClick: onClose, ...props, children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
      "div",
      {
        className: "vocdoni-confirm-modal-content",
        role: "dialog",
        "aria-modal": "true",
        onClick: (event) => event.stopPropagation(),
        children: content
      }
    ) });
  },
  AccountBalance: ({ label, tone, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("span", { ...props, "data-tone": tone, children: label }),
  PaginationContainer: ({ items, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, children: items }),
  PaginationButton: ({ label, href, isActive, disabled, onClick, ...props }) => href ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("a", { ...props, href, "data-active": isActive ? "" : void 0, children: label }) : /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("button", { ...props, onClick, disabled, "data-active": isActive ? "" : void 0, children: label }),
  PaginationEllipsisButton: ({ className, isInput, onToggle, onSubmitPage, placeholder, buttonProps, inputProps }) => {
    if (isInput) {
      return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
        "input",
        {
          className,
          ...inputProps,
          placeholder,
          onKeyDown: (event) => {
            if (event.key !== "Enter" || !(event.currentTarget instanceof HTMLInputElement)) return;
            const pageNumber = Number(event.currentTarget.value);
            onSubmitPage(pageNumber);
          },
          onBlur: onToggle,
          autoFocus: true
        }
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(
      "button",
      {
        className,
        ...buttonProps,
        onClick: (event) => {
          event.preventDefault();
          onToggle();
        },
        children: "..."
      }
    );
  },
  PaginationSummary: ({ text, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("p", { ...props, children: text }),
  OrganizationName: ({ name, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("h1", { ...props, children: name }),
  OrganizationDescription: ({ description, ...props }) => /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(TrustedHtml, { html: description }) }),
  OrganizationAvatar: ({ src, alt, ...props }) => src ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("img", { ...props, src: linkifyIpfs(src), alt }) : null
};

// src/components/context/ComponentsProvider.tsx
var import_jsx_runtime3 = require("react/jsx-runtime");
var ComponentsContext = (0, import_react.createContext)(void 0);
var ComponentsProvider = ({ components, children }) => {
  const merged = (0, import_react.useMemo)(() => ({ ...defaultComponents, ...components || {} }), [components]);
  return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ComponentsContext.Provider, { value: merged, children });
};

// src/components/context/useComponents.ts
var useComponents = () => {
  const context = (0, import_react2.useContext)(ComponentsContext);
  if (!context) {
    throw new Error("useComponents must be used within a <ComponentsProvider />");
  }
  return context;
};

// src/i18n/localize.ts
var import_react_i18next = require("react-i18next");

// src/i18n/locales/en/react-components.json
var react_components_default = {
  balance: "{{ balance }} VOC tokens",
  actions: {
    cancel_description: 'canceling the "{{ election.title.default }}" election process',
    cancel: "Cancel the process immediately, not allowing new votes and not counting any results (caution: it cannot be reverted)",
    cancel_button: "Cancel",
    continue_description: 'resuming the "{{ election.title.default }}" election process',
    continue: "Resume the process immediately, in case it has been paused",
    end_description: 'ending the "{{ election.title.default }}" election process',
    end: "End the process immediately, preventing new votes to be submitted, and shows the voting results (caution: it cannot be reverted)",
    error_title: "There was some error while executing the transaction",
    pause_description: 'pausing the "{{ election.title.default }}" election process',
    pause: "Pause the voting process temporarily, until resumed manually. While the process is paused, voters cannot submit votes.",
    waiting_title: "Waiting for transaction confirmation..."
  },
  confirm: {
    title: "Confirm",
    cancel: "Cancel",
    confirm: "Confirm",
    cancel_process_button: "Cancel process",
    cancel_process_title: "Confirm cancelation",
    end_process_title: "Confirm ending",
    end_process_button: "End process",
    cancel_button: "Cancel"
  },
  empty: "Apparently this process has no questions \u{1F914}",
  errors: {
    unauthorized: "Not authorized to vote",
    wrong_data_title: "Wrong data",
    wrong_data_description: "The specified data is not correct",
    not_voted_in_ended_election: "You did not vote in this election and it's already finished"
  },
  question_types: {
    approval_tooltip: "Approval voting lets you vote for as many options as you like. The one with the most votes wins. It's a simple way to show your support for all the choices you approve of.",
    approval_title: "Approval Voting",
    singlechoice_title: "Single Choice {{ weighted }}",
    multichoice_tooltip: "Multichoice voting lets you vote for up to {{maxcount}} options (defined by the voting organizer). The one with the most votes wins.",
    multichoice_title: "Multichoice Voting {{ weighted }}",
    multichoice_desc: "You selected {{selected}} options (max {{maxcount}})",
    multichoice_desc_abstain: " (or abstain)",
    weighted_voting: "Weighted Voting"
  },
  schedule: {
    from_begin_to_end: "Voting from {{ begin }} to {{ end }}",
    ended: "Ended {{ distance }}",
    paused_start: "(Paused) Starts {{ distance }}",
    paused_end: "(Paused) Ends {{ distance }}",
    created: "Created {{ distance }}"
  },
  results: {
    date_format: "d-L-y HH:mm",
    secret_until_the_end: "Secret until the end. The results will be available when the process ends at {{ endDate }}",
    title: 'Results for "{{ title }}"',
    votes: "Votes: {{ votes }} ({{ percent }})"
  },
  statuses: {
    canceled: "Canceled",
    ended: "Ended",
    ongoing: "Ongoing",
    paused: "Paused",
    results: "Results",
    upcoming: "Upcoming",
    process_unknown: "Unknown",
    invalid: "Invalid"
  },
  spreadsheet: {
    access_button: "Identify",
    anon_sik_label: "Define a password for your vote",
    anon_sik_helper: "This ensures the vote to be anonymous",
    close: "Close",
    logout: "Logout",
    modal_title: "You must identify first"
  },
  pagination: {
    page_placeholder: "Page #",
    total_results: "Showing a total of {{ count }} results",
    previous: "Previous",
    next: "Next"
  },
  validation: {
    required: "This field is required",
    min_length: "This field must be at least {{ min }} characters long",
    choices_count: "Select {{ count }} choices",
    at_least_one: "You must select at least one option"
  },
  vote: {
    abstain: "Abstain",
    button_update: "Re-submit vote",
    button: "Vote",
    confirm: "Please confirm your choices:",
    sign: "Sign first",
    voted_description: "Your vote id is {{ id }}. You can use it to verify your vote.",
    voted_title: "Your vote was successfully cast!",
    weight: "Your voting power is: "
  }
};

// src/i18n/locales.ts
var reactComponentsNamespace = "react-components";
var reactComponentsDefaultLanguage = "en";
var reactComponentsResources = {
  [reactComponentsDefaultLanguage]: {
    [reactComponentsNamespace]: react_components_default
  }
};

// src/i18n/localize.ts
var useReactComponentsLocalize = () => {
  const { t } = (0, import_react_i18next.useTranslation)(reactComponentsNamespace);
  return (key, substitutions) => t(key, substitutions);
};

// src/providers/client/ClientProvider.tsx
var import_react4 = require("react");

// src/providers/client/use-client-provider.ts
var import_react_query = require("@tanstack/react-query");
var import_sdk3 = require("@vocdoni/sdk");
var import_react3 = require("react");

// src/browser.ts
var isBrowser = () => typeof window !== "undefined" && typeof document !== "undefined";
var getStorageItem = (key) => {
  if (!isBrowser()) return null;
  try {
    return window.localStorage.getItem(key);
  } catch {
    return null;
  }
};
var setStorageItem = (key, value) => {
  if (!isBrowser()) return;
  try {
    window.localStorage.setItem(key, value);
  } catch {
  }
};
var removeStorageItem = (key) => {
  if (!isBrowser()) return;
  try {
    window.localStorage.removeItem(key);
  } catch {
  }
};
var getLocationHash = () => {
  if (!isBrowser()) return "";
  return window.location.hash ? window.location.hash.slice(1) : "";
};
var replaceLocationHash = (value) => {
  if (!isBrowser()) return;
  document.location.hash = value;
};
var clearLocationHash = () => {
  if (!isBrowser()) return;
  window.history.pushState({}, "", `${document.location.pathname}${document.location.search}`);
};
var hasEthereumProvider = () => {
  if (!isBrowser()) return false;
  return "ethereum" in window;
};
var canUseWorkers = () => {
  return typeof Worker !== "undefined" && typeof Blob !== "undefined" && typeof URL !== "undefined" && typeof URL.createObjectURL === "function";
};

// src/providers/i18n/localize.tsx
var import_react_i18next2 = require("react-i18next");
var useLocalize = () => {
  const { t } = (0, import_react_i18next2.useTranslation)(reactComponentsNamespace);
  return (key, substitutions) => t(key, substitutions);
};

// src/providers/query/keys.ts
var queryKeys = {
  client: {
    account: (env, address) => ["client", env, address, "account"]
  },
  organization: {
    byId: (id) => ["organization", id]
  },
  election: {
    byId: (id) => ["election", id],
    census: (id, voter) => ["election", id, "census", voter],
    voteInfo: (id, voter) => ["election", id, "vote-info", voter]
  }
};

// src/providers/utils.ts
var import_sdk = require("@vocdoni/sdk");
var import_latinize = __toESM(require("latinize"), 1);
var enforceHexPrefix = (address) => !address ? "" : (0, import_sdk.ensure0x)(address);
var errorToString = (error) => {
  if (typeof error !== "string" && "data" in error && error.data && "error" in error.data) {
    return error.data.error;
  }
  if (typeof error !== "string" && "message" in error) {
    return error.message;
  }
  if (typeof error !== "string" && "reason" in error) {
    return error.reason;
  }
  return error;
};
var normalizeText = (text) => {
  if (!text) return "";
  const result = text.trim().replace(/\s+/g, " ").replace(/[\.·:]/g, ".").replace(/[`´]/g, "'").normalize().toLowerCase();
  return (0, import_latinize.default)(result);
};
var walletFromRow = (salt, row) => {
  const normalized = row.map(normalizeText);
  normalized.push(salt);
  return import_sdk.VocdoniSDKClient.generateWalletFromData(normalized);
};

// src/providers/client/client-utils.ts
var import_sdk2 = require("@vocdoni/sdk");
var CLIENT_ENVS = [import_sdk2.EnvOptions.DEV, import_sdk2.EnvOptions.PROD];
var hasSigner = (wallet) => {
  return !!wallet && typeof wallet.getAddress === "function";
};
var normalizeClientEnv = (env) => {
  if (env === import_sdk2.EnvOptions.PROD) return import_sdk2.EnvOptions.PROD;
  return import_sdk2.EnvOptions.DEV;
};
var newVocdoniSDKClient = (env, signer, options) => {
  return new import_sdk2.VocdoniSDKClient({
    env,
    wallet: signer,
    faucet: options?.faucet_url ? { url: options.faucet_url } : void 0,
    api_url: options?.api_url || void 0
  });
};
var newVocdoniCensus3 = (env, api_url) => {
  return new import_sdk2.VocdoniCensus3Client({ env, api_url });
};

// src/providers/client/use-client-provider.ts
var useClientProvider = ({
  client: c,
  env: e,
  signer: s,
  options: o,
  census3Options: c3o
}) => {
  const localize = useLocalize();
  const queryClient = (0, import_react_query.useQueryClient)();
  const initialEnv = normalizeClientEnv(e);
  const [env, setEnvState] = (0, import_react3.useState)(initialEnv);
  const [signer, setSignerState] = (0, import_react3.useState)(s);
  const [options, setOptionsState] = (0, import_react3.useState)(o);
  const [census3Options, setCensus3OptionsState] = (0, import_react3.useState)(c3o);
  const [client, setClientState] = (0, import_react3.useState)(c || newVocdoniSDKClient(initialEnv, s, o));
  const [census3, setCensus3State] = (0, import_react3.useState)(newVocdoniCensus3(initialEnv, c3o?.api_url));
  const [address, setAddress] = (0, import_react3.useState)(void 0);
  const connected = hasSigner(client?.wallet);
  (0, import_react3.useEffect)(() => {
    let active = true;
    if (!client?.wallet) {
      setAddress(void 0);
      return () => {
        active = false;
      };
    }
    client.wallet.getAddress().then((addr) => {
      if (active) setAddress(addr);
    }).catch(() => {
      if (active) setAddress(void 0);
    });
    return () => {
      active = false;
    };
  }, [client]);
  const accountKey = (0, import_react3.useMemo)(() => {
    if (!address) return null;
    return queryKeys.client.account(String(env), address);
  }, [address, env]);
  const accountQuery = (0, import_react_query.useQuery)({
    queryKey: accountKey || ["client", "account", "disabled"],
    queryFn: async () => {
      return client.fetchAccount(address);
    },
    enabled: false
  });
  const createAccountMutation = (0, import_react_query.useMutation)({
    mutationFn: (opts) => client.createAccount(opts),
    onSuccess: (data) => {
      if (data?.address) {
        setAddress(data.address);
        queryClient.setQueryData(queryKeys.client.account(String(env), data.address), data);
      }
    }
  });
  const updateAccountMutation = (0, import_react_query.useMutation)({
    mutationFn: (account2) => client.updateAccountInfo(account2),
    onSuccess: (data) => {
      if (data?.address) {
        setAddress(data.address);
        queryClient.setQueryData(queryKeys.client.account(String(env), data.address), data);
      }
    }
  });
  const setEnv = (nextEnv) => {
    const normalizedEnv = normalizeClientEnv(nextEnv);
    setEnvState(normalizedEnv);
    setClientState(newVocdoniSDKClient(normalizedEnv, signer, options));
    setCensus3State(newVocdoniCensus3(normalizedEnv, census3Options?.api_url));
    setAddress(void 0);
    queryClient.removeQueries({ queryKey: ["client"] });
  };
  const setSigner = (nextSigner) => {
    setSignerState(nextSigner);
    const nextClient = newVocdoniSDKClient(env, nextSigner, options);
    if (!hasSigner(nextClient.wallet) && hasSigner(nextSigner)) {
      nextClient.wallet = nextSigner;
    }
    setClientState(nextClient);
    setAddress(void 0);
    queryClient.removeQueries({ queryKey: ["client"] });
  };
  const setClient = (nextClient) => {
    setClientState(nextClient);
    setAddress(void 0);
    queryClient.removeQueries({ queryKey: ["client"] });
  };
  (0, import_react3.useEffect)(() => {
    if (!e || e === env) return;
    setEnv(e);
  }, [e]);
  (0, import_react3.useEffect)(() => {
    if (!s || s === signer) return;
    setSigner(s);
  }, [s]);
  (0, import_react3.useEffect)(() => {
    if (o === options) return;
    const signerFromProps = hasSigner(s) ? s : void 0;
    const signerToUse = signerFromProps || signer;
    setOptionsState(o);
    const nextClient = newVocdoniSDKClient(env, signerToUse, o);
    if (!hasSigner(nextClient.wallet) && hasSigner(signerToUse)) {
      nextClient.wallet = signerToUse;
    }
    setClientState(nextClient);
  }, [o]);
  (0, import_react3.useEffect)(() => {
    if (c3o === census3Options) return;
    setCensus3OptionsState(c3o);
    setCensus3State(newVocdoniCensus3(env, c3o?.api_url));
  }, [c3o]);
  (0, import_react3.useEffect)(() => {
    if (!c || c === client) return;
    setClientState(c);
    setAddress(void 0);
    queryClient.removeQueries({ queryKey: ["client"] });
  }, [c]);
  (0, import_react3.useEffect)(() => {
    if (!hasEthereumProvider()) return;
    const accChanged = async () => {
      setClientState(newVocdoniSDKClient(env, signer, options));
      setAddress(void 0);
      queryClient.removeQueries({ queryKey: ["client"] });
    };
    window.ethereum.on("accountsChanged", accChanged);
    return () => {
      ;
      window.ethereum.removeListener("accountsChanged", accChanged);
    };
  }, []);
  (0, import_react3.useEffect)(() => {
    if (!connected || accountQuery.data) return;
    fetchAccount();
  }, [connected, env, signer, address]);
  const fetchAccount = async () => {
    if (!client.wallet) return;
    if (accountQuery.isFetching) return;
    const nextAddress = address || await client.wallet.getAddress();
    if (!nextAddress) return;
    if (nextAddress !== address) setAddress(nextAddress);
    return queryClient.fetchQuery({
      queryKey: queryKeys.client.account(String(env), nextAddress),
      queryFn: () => client.fetchAccount(nextAddress)
    });
  };
  const createAccount = (opts) => {
    if (createAccountMutation.isPending) return;
    return createAccountMutation.mutateAsync(opts);
  };
  const updateAccount = (account2) => {
    if (updateAccountMutation.isPending) return;
    return updateAccountMutation.mutateAsync(account2);
  };
  const generateSigner = (seed) => {
    if (!client) {
      throw new Error("No client initialized");
    }
    let wallet;
    if (!seed) {
      client.generateRandomWallet();
      wallet = client.wallet;
    } else {
      wallet = import_sdk3.VocdoniSDKClient.generateWalletFromData(seed);
    }
    return wallet;
  };
  const account = accountQuery.data;
  const balance = account?.balance ?? -1;
  const loading = {
    account: accountQuery.isFetching || createAccountMutation.isPending || updateAccountMutation.isPending,
    fetch: accountQuery.isFetching,
    create: createAccountMutation.isPending,
    update: updateAccountMutation.isPending
  };
  const loaded = {
    account: accountQuery.isSuccess || accountQuery.isError || createAccountMutation.isSuccess || updateAccountMutation.isSuccess,
    fetch: accountQuery.isSuccess || accountQuery.isError,
    create: createAccountMutation.isSuccess || createAccountMutation.isError,
    update: updateAccountMutation.isSuccess || updateAccountMutation.isError
  };
  const errors = {
    account: accountQuery.error ? errorToString(accountQuery.error) : null,
    fetch: accountQuery.error ? errorToString(accountQuery.error) : null,
    create: createAccountMutation.error ? errorToString(createAccountMutation.error) : null,
    update: updateAccountMutation.error ? errorToString(updateAccountMutation.error) : null
  };
  const clear = () => {
    queryClient.removeQueries({ queryKey: ["client"] });
    setSignerState(null);
    setClientState(newVocdoniSDKClient(env, void 0, options));
    setAddress(void 0);
  };
  return {
    env,
    signer,
    client,
    census3,
    account,
    balance,
    connected,
    loading,
    loaded,
    errors,
    options,
    census3Options,
    clear,
    setClient,
    setSigner,
    setEnv,
    createAccount,
    fetchAccount,
    generateSigner,
    localize,
    updateAccount
  };
};

// src/providers/client/ClientProvider.tsx
var import_jsx_runtime4 = require("react/jsx-runtime");
var ClientContext = (0, import_react4.createContext)(void 0);
var useClient = () => {
  const ctxt = (0, import_react4.useContext)(ClientContext);
  if (!ctxt) {
    throw new Error("useClient returned `undefined`, maybe you forgot to wrap the component within <ClientProvider />?");
  }
  return {
    ...ctxt,
    // Allow client extensions
    client: ctxt.client
  };
};
var InternalClientProvider = ({ env, client, signer, ...rest }) => {
  const value = useClientProvider({ env, client, signer, ...rest });
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(ClientContext.Provider, { value, ...rest });
};
var ClientProvider = (props) => {
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(InternalClientProvider, { ...props });
};

// src/providers/election/ActionsProvider.tsx
var import_react5 = require("react");
var import_jsx_runtime5 = require("react/jsx-runtime");
var BaseLoadingState = {
  continue: false,
  pause: false,
  end: false,
  cancel: false
};
var useActionsProvider = () => {
  const { localize } = useClient();
  const { client, election, fetchElection } = useElection();
  const [loading, setLoading] = (0, import_react5.useState)(BaseLoadingState);
  const [error, setError] = (0, import_react5.useState)(null);
  const [info, setInfo] = (0, import_react5.useState)(null);
  const load = (key) => setLoading((loading2) => ({ ...loading2, [key]: true }));
  const close = () => {
    setLoading(BaseLoadingState);
    setInfo(null);
  };
  const infostate = (description) => {
    setInfo({
      title: localize("actions.waiting_title"),
      description
    });
  };
  const errorstate = (description) => {
    setError({
      title: localize("actions.error_title"),
      description
    });
  };
  const cancel = async () => {
    if (!election) {
      throw new Error("no election in provider");
    }
    load("cancel");
    infostate(
      localize("actions.cancel_description", {
        election
      })
    );
    try {
      await client.cancelElection(election.id);
      await fetchElection(election.id);
    } catch (e) {
      if (typeof e === "string") {
        return errorstate(e);
      }
      console.warn('catched error in "cancel" action', e);
    } finally {
      close();
    }
  };
  const end = async () => {
    if (!election) {
      throw new Error("no election in provider");
    }
    load("end");
    infostate(
      localize("actions.end_description", {
        election
      })
    );
    try {
      await client.endElection(election.id);
      await fetchElection(election.id);
    } catch (e) {
      if (typeof e === "string") {
        return errorstate(e);
      }
      console.warn('catched error in "end" action', e);
    } finally {
      close();
    }
  };
  const pause = async () => {
    if (!election) {
      throw new Error("no election in provider");
    }
    load("pause");
    infostate(
      localize("actions.pause_description", {
        election
      })
    );
    try {
      await client.pauseElection(election.id);
      await fetchElection(election.id);
    } catch (e) {
      if (typeof e === "string") {
        return errorstate(e);
      }
      console.warn('catched error in "pause" action', e);
    } finally {
      close();
    }
  };
  const resume = async () => {
    if (!election) {
      throw new Error("no election in provider");
    }
    load("continue");
    infostate(
      localize("actions.continue_description", {
        election
      })
    );
    try {
      await client.continueElection(election.id);
      await fetchElection(election.id);
    } catch (e) {
      if (typeof e === "string") {
        return errorstate(e);
      }
      console.warn('catched error in "continue" action', e);
    } finally {
      close();
    }
  };
  const disabled = Object.values(loading).some((value) => value === true);
  (0, import_react5.useEffect)(() => {
    if (election && !disabled) {
      setError(null);
    }
  }, [disabled, election]);
  return {
    disabled,
    cancel,
    end,
    error,
    info,
    loading,
    pause,
    resume
  };
};
var ActionsContext = (0, import_react5.createContext)(void 0);
var useActions = () => {
  const ctxt = (0, import_react5.useContext)(ActionsContext);
  if (!ctxt) {
    throw new Error(
      "useActions returned `undefined`, maybe you forgot to wrap the component within <ActionsProvider />?"
    );
  }
  return ctxt;
};
var ActionsProvider = (props) => {
  const value = useActionsProvider();
  return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(ActionsContext.Provider, { value, ...props });
};
ActionsProvider.displayName = "ActionsProvider";

// src/providers/election/ElectionProvider.tsx
var import_react9 = require("react");

// src/providers/election/use-election-provider.ts
var import_react_query2 = require("@tanstack/react-query");
var import_sdk7 = require("@vocdoni/sdk");
var import_react8 = require("react");

// src/providers/csp/index.ts
var import_sdk4 = require("@vocdoni/sdk");
var import_up_fetch = require("up-fetch");
var f = (0, import_up_fetch.up)(fetch);
var vote = async (client, election, token, vote2) => {
  if (!client) {
    throw new Error("no client initialized");
  }
  if (!(election instanceof import_sdk4.PublishedElection) || election?.census?.type !== import_sdk4.CensusType.CSP) {
    throw new Error("not a CSP election");
  }
  const walletAddress = await client.wallet?.getAddress();
  const { signature, weight } = await f(`${election.census.censusURI}/sign`, {
    method: "POST",
    body: {
      payload: walletAddress,
      authToken: token,
      electionId: election.id
    }
  });
  const cspVote = client.cspVote(vote2, signature, import_sdk4.CspProofType.ECDSA_PIDSALTED);
  if (typeof weight !== "undefined") {
    const parsedWeight = Number.parseInt(weight, 16);
    cspVote.weight = Number.isNaN(parsedWeight) ? BigInt(1) : BigInt(parsedWeight);
  }
  const vid = await client.submitVote(cspVote);
  return vid;
};
var fetchSignInfo = ({
  endpoint,
  processId,
  authToken
}) => f(`${endpoint}/process/${processId}/sign-info`, {
  method: "POST",
  body: { authToken }
});

// src/providers/worker/circuitWorkerScript.ts
var circuitWorkerScript_default = () => {
  self.addEventListener("message", async (e) => {
    try {
      const { circuits } = e.data;
      async function fetchDataInChunks(uri) {
        const response = await fetch(uri);
        const reader = response.body?.getReader();
        const contentLength = +(response.headers?.get("Content-Length") ?? 0);
        const chunks = [];
        let receivedLength = 0;
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          chunks.push(value);
          receivedLength += value.length;
          if (contentLength && receivedLength >= contentLength) break;
        }
        const concatenatedArray = new Uint8Array(receivedLength);
        let offset = 0;
        for (const chunk of chunks) {
          concatenatedArray.set(chunk, offset);
          offset += chunk.length;
        }
        return concatenatedArray;
      }
      const [zKeyData, vKeyData, wasmData] = await Promise.all([
        fetchDataInChunks(circuits.zKeyURI),
        fetchDataInChunks(circuits.vKeyURI),
        fetchDataInChunks(circuits.wasmURI)
      ]);
      const circuitsData = {
        zKeyData,
        zKeyURI: circuits.zKeyURI,
        zKeyHash: circuits.zKeyHash,
        vKeyData,
        vKeyURI: circuits.vKeyURI,
        vKeyHash: circuits.vKeyHash,
        wasmData,
        wasmURI: circuits.wasmURI,
        wasmHash: circuits.wasmHash
      };
      return postMessage({ result: circuitsData });
    } catch (error) {
      return postMessage({ error });
    }
  });
};

// src/providers/worker/useWebWorker.ts
var import_react6 = require("react");
var useWebWorker = (worker) => {
  const [running, setRunning] = (0, import_react6.useState)(false);
  const [error, setError] = (0, import_react6.useState)();
  const [result, setResult] = (0, import_react6.useState)();
  const startProcessing = (0, import_react6.useCallback)(
    (data) => {
      if (!worker) return;
      setRunning(true);
      worker.postMessage(data);
    },
    [worker]
  );
  (0, import_react6.useEffect)(() => {
    if (!worker) return;
    const onMessage = (event) => {
      setRunning(false);
      setError(event.data.error);
      setResult(event.data.result);
    };
    worker.addEventListener("message", onMessage);
    return () => worker.removeEventListener("message", onMessage);
  }, [worker]);
  return {
    startProcessing,
    running,
    error,
    result
  };
};

// src/providers/worker/webWorker.ts
var createWebWorker = (worker) => {
  if (typeof Worker === "undefined" || typeof Blob === "undefined" || typeof URL === "undefined") {
    return null;
  }
  const workerURL = globalThis.MockedWindowURL || globalThis.URL;
  if (typeof workerURL?.createObjectURL !== "function") {
    return null;
  }
  const code = worker.toString();
  const blob = new Blob(["(" + code + ")()"]);
  const blobURL = workerURL.createObjectURL(blob);
  const instance = new Worker(blobURL);
  const terminate = instance.terminate?.bind(instance);
  instance.terminate = () => {
    terminate?.();
    workerURL.revokeObjectURL?.(blobURL);
  };
  return instance;
};

// src/providers/election/normalized.ts
var import_sdk5 = require("@vocdoni/sdk");
var hasConstructorName = (value, name) => isObject(value) && typeof value.constructor?.name === "string" && value.constructor.name === name;
var isObject = (value) => typeof value === "object" && value !== null;
var withDefaultText = (value) => {
  if (isObject(value) && isObject(value.default)) return value;
  if (typeof value === "string") return { default: value };
  return value;
};
var toPublicCensusShape = (census) => {
  if (!isObject(census)) return census;
  return {
    ...census,
    type: census.type ?? census._type,
    size: census.size ?? census._size,
    weight: census.weight ?? census._weight,
    censusId: census.censusId ?? census._censusId,
    censusURI: census.censusURI ?? census._censusURI,
    salt: census.salt ?? census._salt,
    participants: census.participants ?? census._participants
  };
};
var toPublicElectionTypeShape = (value) => {
  if (!isObject(value)) return value;
  return {
    ...value,
    anonymous: value.anonymous ?? value._anonymous,
    secretUntilTheEnd: value.secretUntilTheEnd ?? value._secretUntilTheEnd
  };
};
var toPublicVoteTypeShape = (value) => {
  if (!isObject(value)) return value;
  return {
    ...value,
    maxCount: value.maxCount ?? value._maxCount,
    maxVoteOverwrites: value.maxVoteOverwrites ?? value._maxVoteOverwrites
  };
};
var toPublicResultsTypeShape = (value) => {
  if (!isObject(value)) return value;
  return {
    ...value,
    name: value.name ?? value._name,
    properties: value.properties ?? value._properties
  };
};
var toPublicElectionShape = (election) => {
  if (!isObject(election)) return election;
  const source = election;
  const snapshot = {
    ...source,
    id: source.id ?? source._id,
    title: withDefaultText(source.title ?? source._title),
    description: withDefaultText(source.description ?? source._description),
    header: source.header ?? source._header,
    startDate: source.startDate ?? source._startDate,
    endDate: source.endDate ?? source._endDate,
    creationTime: source.creationTime ?? source._creationTime,
    status: source.status ?? source._status,
    questions: source.questions ?? source._questions ?? [],
    census: toPublicCensusShape(source.census ?? source._census),
    electionType: toPublicElectionTypeShape(source.electionType ?? source._electionType),
    voteType: toPublicVoteTypeShape(source.voteType ?? source._voteType),
    resultsType: toPublicResultsTypeShape(source.resultsType ?? source._resultsType),
    meta: source.meta ?? source._meta,
    organizationId: source.organizationId ?? source._organizationId,
    results: source.results ?? source._results,
    voteCount: source.voteCount ?? source._voteCount,
    maxCensusSize: source.maxCensusSize ?? source._maxCensusSize
  };
  return {
    ...snapshot,
    get: (path) => getElectionField(snapshot, path)
  };
};
var getElectionField = (election, path) => {
  if (!election) return void 0;
  if (typeof election.get === "function") {
    try {
      return election.get(path);
    } catch {
    }
  }
  return path.split(".").reduce((value, key) => {
    if (!isObject(value)) return void 0;
    return value[key];
  }, election);
};
var getElectionTitle = (election) => {
  const source = election;
  return source?.title?.default || source?._title?.default || source?.id || source?._id;
};
var getElectionDescription = (election) => {
  const source = election;
  return source?.description?.default || source?._description?.default;
};
var getElectionStatus = (election) => {
  const source = election;
  return source?.status || source?._status;
};
var getElectionDate = (election, field) => {
  const value = election?.[field];
  if (!value) return void 0;
  return value instanceof Date ? value : new Date(value);
};
var isInvalidElectionLike = (election) => {
  return hasConstructorName(election, "InvalidElection") || getElectionStatus(election) === "PROCESS_UNKNOWN";
};
var isPublishedElectionLike = (election) => {
  return !!election && !isInvalidElectionLike(election);
};
var normalizeElection = (election) => {
  if (!election) return void 0;
  if (election instanceof import_sdk5.PublishedElection || hasConstructorName(election, "InvalidElection")) return election;
  const normalized = toPublicElectionShape(election);
  const builder = import_sdk5.PublishedElection.build;
  if (typeof builder === "function") {
    try {
      const revived = builder(normalized);
      if (!hasConstructorName(revived, "InvalidElection")) {
        return revived;
      }
    } catch {
    }
  }
  return normalized;
};

// src/providers/election/use-election-reducer.ts
var import_sdk6 = require("@vocdoni/sdk");
var import_react7 = require("react");
var CensusClear = "election:census:clear";
var CensusIsAbleToVote = "election:census:is_able_to_vote";
var CensusLoad = "election:census:load";
var ElectionClientSet = "election:client:set";
var ElectionCspStep0 = "election:csp:step_0";
var ElectionCspStep1 = "election:csp:step_1";
var ElectionInCensus = "election:in_census";
var ElectionLoad = "election:load";
var ElectionSet = "election:set";
var ElectionVoted = "election:voted";
var ElectionVoteSet = "election:vote:set";
var ElectionVotesLeft = "election:votes_left";
var ElectionVoting = "election:voting";
var SikPasswordSet = "sik:password:set";
var SikSignatureSet = "sik:signature:set";
var participation = (election) => {
  const current = election;
  if (!current || isInvalidElectionLike(election) || !current.census && !current.maxCensusSize) {
    return 0;
  }
  const size = current.census && current.census.size ? current.census.size : current.maxCensusSize;
  return Math.round((current.voteCount || 0) / size * 1e4) / 100;
};
var turnout = (election) => {
  const current = election;
  if (!current || isInvalidElectionLike(election) || !current.census && !current.maxCensusSize) {
    return 0;
  }
  const size = current.census && current.census.size ? current.census.size : current.maxCensusSize;
  const totalVotes = current.results ? current.results.reduce((acc, questionResults) => {
    return acc + questionResults.reduce((sum, value) => sum + Number(value), 0);
  }, 0) : current.voteCount || 0;
  return Math.round(totalVotes / size * 1e4) / 100;
};
var electionStateEmpty = ({
  client,
  election
}) => ({
  client,
  connected: false,
  id: election?.id || void 0,
  isAbleToVote: void 0,
  isInCensus: false,
  election,
  vote: void 0,
  voter: void 0,
  votesLeft: 0,
  voted: null,
  voting: false,
  csp: {
    token: void 0
  },
  sik: {
    password: void 0,
    signature: void 0
  },
  participation: participation(election),
  turnout: turnout(election)
});
var isAbleToVote = (state, payload) => {
  if (typeof payload === "boolean") {
    return payload;
  }
  if (!(state.election instanceof import_sdk6.PublishedElection)) {
    return null;
  }
  const hasCspAccess = !!state.csp.token && state.isInCensus;
  const canOverwrite = (state.election.voteType?.maxVoteOverwrites ?? 0) > 0;
  return state.isInCensus && state.votesLeft > 0 || hasCspAccess && (!state.voted || state.voted && canOverwrite) || state.isInCensus && state.election?.electionType.anonymous && !state.voted;
};
var electionReducer = (state, action) => {
  switch (action.type) {
    case CensusClear: {
      return electionStateEmpty({ client: state.client, election: state.election });
    }
    case CensusLoad: {
      const payload = action.payload;
      return {
        ...state,
        voter: payload
      };
    }
    case CensusIsAbleToVote: {
      const payload = action.payload;
      return {
        ...state,
        isAbleToVote: isAbleToVote(state, payload)
      };
    }
    case ElectionCspStep0: {
      const data = action.payload;
      return {
        ...state,
        csp: {
          ...state.csp,
          ...data
        }
      };
    }
    case ElectionCspStep1: {
      const token = action.payload;
      const rstate = {
        ...state,
        csp: {
          ...state.csp,
          token
        }
      };
      return {
        ...rstate,
        isAbleToVote: isAbleToVote(rstate)
      };
    }
    case ElectionLoad:
      const id = action.payload;
      return {
        ...state,
        id
      };
    case ElectionSet: {
      const election = action.payload;
      return {
        ...state,
        election,
        participation: participation(election),
        turnout: turnout(election)
      };
    }
    case ElectionInCensus: {
      const isInCensus = action.payload;
      const rstate = {
        ...state,
        isInCensus
      };
      return {
        ...rstate,
        isAbleToVote: isAbleToVote(rstate)
      };
    }
    case ElectionVoted: {
      const voted = action.payload;
      const shouldDecrement = !!voted && state.voting;
      const nextVotesLeft = shouldDecrement ? Math.max(0, state.votesLeft - 1) : state.votesLeft;
      const rstate = {
        ...state,
        voted,
        votesLeft: nextVotesLeft,
        vote: void 0,
        voting: false
      };
      return {
        ...rstate,
        isAbleToVote: isAbleToVote(rstate)
      };
    }
    case ElectionVoteSet: {
      const vote2 = action.payload;
      return {
        ...state,
        vote: vote2
      };
    }
    case ElectionVoting: {
      return {
        ...state,
        voted: null,
        voting: true
      };
    }
    case ElectionVotesLeft: {
      const votesLeft = action.payload;
      const rstate = {
        ...state,
        votesLeft
      };
      return {
        ...rstate,
        isAbleToVote: isAbleToVote(rstate)
      };
    }
    case ElectionClientSet: {
      const client = action.payload;
      return {
        ...state,
        client,
        connected: hasSigner(client?.wallet)
      };
    }
    case SikPasswordSet: {
      const password = action.payload;
      return {
        ...state,
        sik: {
          ...state.sik,
          password
        }
      };
    }
    case SikSignatureSet: {
      const signature = action.payload;
      return {
        ...state,
        sik: {
          ...state.sik,
          signature
        }
      };
    }
    default:
      return state;
  }
};
var useElectionReducer = (client, election) => {
  const initial = electionStateEmpty({ client, election });
  const { connected } = useClient();
  const [storageHydrated, setStorageHydrated] = (0, import_react7.useState)(false);
  const [state, dispatch] = (0, import_react7.useReducer)(electionReducer, {
    ...initial,
    election
  });
  const previousGlobalClient = (0, import_react7.useRef)(client);
  const isLocalWalletSigner = state.election instanceof import_sdk6.PublishedElection && (state.election.census?.type === import_sdk6.CensusType.CSP || getElectionField(state.election, "census.type") === "spreadsheet");
  (0, import_react7.useEffect)(() => {
    const token = getStorageItem("csp_token" /* tokenR */) || void 0;
    if (token) {
      dispatch({ type: ElectionCspStep0, payload: { token } });
    }
    setStorageHydrated(true);
  }, []);
  (0, import_react7.useEffect)(() => {
    if (!storageHydrated) return;
    if (state.csp.token) {
      setStorageItem("csp_token" /* tokenR */, state.csp.token);
      return;
    }
    removeStorageItem("csp_token" /* tokenR */);
  }, [state.csp.token, storageHydrated]);
  (0, import_react7.useEffect)(() => {
    if (!client) return;
    const electionHasWallet = hasSigner(state.client?.wallet);
    const globalHasWallet = hasSigner(client?.wallet);
    if (isLocalWalletSigner && (electionHasWallet || !globalHasWallet)) {
      return;
    }
    const globalClientChanged = previousGlobalClient.current !== client;
    previousGlobalClient.current = client;
    if (globalClientChanged || state.connected !== connected) {
      dispatch({ type: ElectionClientSet, payload: client });
    }
  }, [client, connected, isLocalWalletSigner, state.connected, state.election]);
  (0, import_react7.useEffect)(() => {
    if (!election) return;
    if (state.election && (0, import_sdk6.areEqualHexStrings)(state.election?.id, election.id)) return;
    dispatch({ type: ElectionSet, payload: election });
  }, [election?.id, state.election]);
  (0, import_react7.useEffect)(() => {
    if (state.connected && connected || !(state.election instanceof import_sdk6.PublishedElection)) return;
    if (!state.election?.meta?.census) {
      return;
    }
    if (
      // we don't want to disconnect the local client for Wallet sessions when the main client gets disconnected
      !connected && isLocalWalletSigner || // we don't want to clear the local client on Signer sessions (non wallet ones)
      !state.connected && !isLocalWalletSigner
    ) {
      return;
    }
    dispatch({ type: CensusClear });
  }, [state.connected, connected, state.election]);
  const actions = (0, import_react7.useMemo)(
    () => ({
      clear: () => dispatch({ type: CensusClear }),
      set: (election2) => dispatch({ type: ElectionSet, payload: election2 }),
      setClient: (client2) => dispatch({ type: ElectionClientSet, payload: client2 }),
      sikPassword: (password) => dispatch({ type: SikPasswordSet, payload: password }),
      sikSignature: (signature) => dispatch({ type: SikSignatureSet, payload: signature }),
      clearClient: () => {
        dispatch({ type: ElectionClientSet, payload: client });
        dispatch({ type: SikPasswordSet, payload: void 0 });
        dispatch({ type: SikSignatureSet, payload: void 0 });
        dispatch({ type: CensusClear });
      },
      load: (id) => dispatch({ type: ElectionLoad, payload: id }),
      csp0: (data) => dispatch({ type: ElectionCspStep0, payload: data }),
      csp1: (token) => dispatch({ type: ElectionCspStep1, payload: token }),
      loadCensus: (voter) => dispatch({ type: CensusLoad, payload: voter }),
      setVote: (vote2) => dispatch({ type: ElectionVoteSet, payload: vote2 }),
      inCensus: (isIn) => dispatch({ type: ElectionInCensus, payload: isIn }),
      isAbleToVote: (isAble) => dispatch({ type: CensusIsAbleToVote, payload: isAble }),
      voted: (voted) => dispatch({ type: ElectionVoted, payload: voted }),
      voting: () => dispatch({ type: ElectionVoting }),
      votesLeft: (votesLeft) => dispatch({ type: ElectionVotesLeft, payload: votesLeft })
    }),
    [client, dispatch]
  );
  return {
    state,
    dispatch,
    actions
  };
};

// src/providers/election/use-election-provider.ts
var getIsWeighted = (election) => {
  const current = election;
  const rawWeight = current?.census?.weight;
  const rawSize = current?.census?.size;
  if (typeof rawWeight === "undefined" || rawWeight === null || typeof rawSize === "undefined" || rawSize === null) {
    return false;
  }
  const weight = Number(rawWeight);
  const size = Number(rawSize);
  if (!Number.isFinite(weight) || !Number.isFinite(size)) {
    return false;
  }
  return weight !== size;
};
var useElectionProvider = ({
  id,
  election: data,
  queryOptions,
  fetchCensus,
  beforeSubmit,
  ...rest
}) => {
  const { client: c, localize, generateSigner } = useClient();
  const queryClient = (0, import_react_query2.useQueryClient)();
  const normalizedData = (0, import_react8.useMemo)(() => normalizeElection(data), [data]);
  const initialElectionId = normalizedData?.id;
  const { state, actions } = useElectionReducer(c, normalizedData);
  const {
    client,
    election,
    sik: { password, signature }
  } = state;
  const [anonCircuitsFetched, setAnonCircuitsFetched] = (0, import_react8.useState)(false);
  const [loading, setLoading] = (0, import_react8.useState)({
    census: false,
    election: false,
    voting: false
  });
  const [loaded, setLoaded] = (0, import_react8.useState)({
    census: false,
    election: !!data,
    voted: false
  });
  const [errors, setErrors] = (0, import_react8.useState)({
    census: null,
    election: null,
    voting: null
  });
  const isAnonCircuitsFetching = (0, import_react8.useRef)(false);
  const [workerInstance, setWorkerInstance] = (0, import_react8.useState)(null);
  const setVoted = (0, import_react8.useCallback)(
    (voteId) => {
      actions.voted(voteId);
      setLoaded((prev) => ({ ...prev, census: true, voted: true }));
      setLoading((prev) => ({ ...prev, census: false, voting: false }));
    },
    [actions]
  );
  const setIsAbleToVote = (0, import_react8.useCallback)(
    (payload) => {
      actions.isAbleToVote(payload);
      setLoaded((prev) => ({ ...prev, census: true }));
      setLoading((prev) => ({ ...prev, census: false }));
    },
    [actions]
  );
  const currentElectionId = id;
  const electionQueryKey = currentElectionId ? queryKeys.election.byId(currentElectionId) : ["election", "disabled"];
  const electionQuery = (0, import_react_query2.useQuery)({
    queryKey: electionQueryKey,
    queryFn: () => client.fetchElection(currentElectionId),
    enabled: !!currentElectionId && !!client,
    initialData: normalizedData,
    ...queryOptions
  });
  const fetchElection = (0, import_react8.useCallback)(
    async (id2) => {
      actions.load(id2);
      setLoading((prev) => ({ ...prev, election: true }));
      setErrors((prev) => ({ ...prev, election: null }));
      try {
        if (currentElectionId && id2 === currentElectionId && electionQuery.refetch) {
          const { data: refreshed } = await electionQuery.refetch();
          if (refreshed) {
            actions.set(normalizeElection(refreshed));
          }
        } else {
          const fetched = await queryClient.fetchQuery({
            queryKey: queryKeys.election.byId(id2),
            queryFn: () => client.fetchElection(id2)
          });
          actions.set(normalizeElection(fetched));
        }
        setLoaded((prev) => ({ ...prev, election: true }));
        setLoading((prev) => ({ ...prev, election: false }));
      } catch (e) {
        setLoaded((prev) => ({ ...prev, election: true }));
        setLoading((prev) => ({ ...prev, election: false }));
        setErrors((prev) => ({ ...prev, election: errorToString(e) }));
      }
    },
    [actions, client, currentElectionId, electionQuery, queryClient]
  );
  const censusFetch = (0, import_react8.useCallback)(async () => {
    const address = await client.wallet?.getAddress();
    if (!isPublishedElectionLike(election)) return;
    const current = election;
    try {
      setLoading((prev) => ({ ...prev, census: true }));
      setErrors((prev) => ({ ...prev, census: null }));
      actions.loadCensus(address);
      const censusType = current.census?.type;
      const isAnonymousElection = !!current.electionType?.anonymous;
      const isCsp = censusType === import_sdk7.CensusType.CSP;
      const isIn = isCsp ? !!state.csp.token : await client.isInCensus({ electionId: election.id });
      actions.inCensus(isIn);
      const requestData = {
        electionId: election.id
      };
      if (isIn && isAnonymousElection && signature) {
        requestData.voteId = await import_sdk7.AnonymousService.calcVoteId(signature, password ?? "0", election.id);
      }
      if (isIn && censusType === import_sdk7.CensusType.WEIGHTED && !isAnonymousElection) {
        setVoted(await client.hasAlreadyVoted(requestData));
        actions.votesLeft(await client.votesLeftCount(requestData));
      }
      setIsAbleToVote();
    } catch (e) {
      console.error("error in census fetch:", e);
      setErrors((prev) => ({ ...prev, census: errorToString(e) }));
      setLoading((prev) => ({ ...prev, census: false }));
      setLoaded((prev) => ({ ...prev, census: true }));
    }
  }, [actions, client, election, password, signature, setIsAbleToVote, setVoted, state.csp.token]);
  const { result: circuits, startProcessing } = useWebWorker(workerInstance);
  (0, import_react8.useEffect)(() => {
    if (workerInstance || !fetchCensus || !isPublishedElectionLike(election)) return;
    if (election.census?.type !== import_sdk7.CensusType.ANONYMOUS || !canUseWorkers()) return;
    const instance = createWebWorker(circuitWorkerScript_default);
    setWorkerInstance(instance);
    return () => {
      instance?.terminate?.();
    };
  }, [election, fetchCensus, workerInstance]);
  const fetchAnonCircuits = (0, import_react8.useCallback)(async () => {
    if (!isPublishedElectionLike(election)) return;
    const current = election;
    const maxVoteOverwrites = Number(current.voteType?.maxVoteOverwrites ?? 0);
    const hasOverwriteEnabled = Number.isFinite(maxVoteOverwrites) && maxVoteOverwrites > 0;
    const votable = state.isAbleToVote || hasOverwriteEnabled && state.isInCensus && state.voted;
    if (votable && current.census?.type === import_sdk7.CensusType.ANONYMOUS) {
      const chainCircuits = await import_sdk7.ChainAPI.circuits(client.url);
      const circuits2 = {
        zKeyURI: chainCircuits.uri + "/" + chainCircuits.circuitPath + "/" + chainCircuits.zKeyFilename,
        zKeyHash: chainCircuits.zKeyHash,
        vKeyURI: chainCircuits.uri + "/" + chainCircuits.circuitPath + "/" + chainCircuits.vKeyFilename,
        vKeyHash: chainCircuits.vKeyHash,
        wasmURI: chainCircuits.uri + "/" + chainCircuits.circuitPath + "/" + chainCircuits.wasmFilename,
        wasmHash: chainCircuits.wasmHash
      };
      startProcessing({ circuits: circuits2 });
    }
  }, [client.url, election, startProcessing, state.isAbleToVote, state.isInCensus, state.voted, workerInstance]);
  (0, import_react8.useEffect)(() => {
    if (!fetchCensus || !election || loading.census || !client.wallet || anonCircuitsFetched || isAnonCircuitsFetching.current) {
      return;
    }
    if (!isPublishedElectionLike(election)) return;
    if (election.census?.type !== import_sdk7.CensusType.ANONYMOUS) return;
    isAnonCircuitsFetching.current = true;
    fetchAnonCircuits();
  }, [anonCircuitsFetched, client.wallet, election, fetchAnonCircuits, fetchCensus, loading.census]);
  (0, import_react8.useEffect)(() => {
    if (!state.csp.token || state.connected || !state.election) return;
    const signer = generateSigner();
    client.wallet = signer;
    actions.setClient(client);
  }, [actions, client, generateSigner, state.connected, state.csp.token, state.election]);
  (0, import_react8.useEffect)(() => {
    ;
    (async () => {
      if (!circuits) return;
      setAnonCircuitsFetched(true);
      client.anonymousService.setCircuits(circuits);
    })();
  }, [circuits]);
  (0, import_react8.useEffect)(() => {
    if (!id) return;
    if (state.id && (0, import_sdk7.areEqualHexStrings)(state.id, id)) return;
    actions.load(id);
  }, [actions, id, state.id]);
  (0, import_react8.useEffect)(() => {
    if (!id || !client) return;
    if (electionQuery.isFetching) {
      setLoading((prev) => ({ ...prev, election: true }));
    }
    if (electionQuery.data) {
      actions.set(normalizeElection(electionQuery.data));
      setLoaded((prev) => ({ ...prev, election: true }));
      setLoading((prev) => ({ ...prev, election: false }));
    }
    if (electionQuery.isError) {
      setLoaded((prev) => ({ ...prev, election: true }));
      setLoading((prev) => ({ ...prev, election: false }));
      setErrors((prev) => ({ ...prev, election: errorToString(electionQuery.error) }));
    }
  }, [actions, client, electionQuery.data, electionQuery.error, electionQuery.isError, electionQuery.isFetching, id]);
  (0, import_react8.useEffect)(() => {
    if (!fetchCensus || !election || !loaded.election || loading.census || !client.wallet) return;
    (async () => {
      const address = await client.wallet?.getAddress();
      if (!(!loaded.census || !(0, import_sdk7.areEqualHexStrings)(state.voter, address) || initialElectionId && !(0, import_sdk7.areEqualHexStrings)(initialElectionId, election.id) || // fetch census if there's sik signature and no vote id
      signature && !loaded.voted)) {
        return;
      }
      await censusFetch();
    })();
  }, [fetchCensus, client, state.voter, loaded.election, loading.census, actions, state.isAbleToVote, signature, initialElectionId]);
  (0, import_react8.useEffect)(() => {
    if (!state.csp.token || !election || !loaded.election || isInvalidElectionLike(election)) return;
    (async () => {
      try {
        actions.inCensus(true);
        const uri = new URL(election.census.censusURI);
        const endpoint = `${uri.protocol}//${uri.host}`;
        let nullifier = null;
        try {
          const signInfo = await fetchSignInfo({
            endpoint,
            authToken: state.csp.token,
            processId: election.id
          });
          nullifier = signInfo.nullifier;
        } catch (error) {
          const status = error?.status ?? error?.response?.status ?? error?.cause?.status;
          if (Number(status) === 401) {
            const maxVoteOverwrites2 = election.voteType?.maxVoteOverwrites ?? 0;
            actions.votesLeft(maxVoteOverwrites2 + 1);
            return;
          }
          throw error;
        }
        let voteInfo = null;
        try {
          voteInfo = await client.voteService.info(nullifier);
        } catch (e) {
          voteInfo = null;
        }
        const voteId = voteInfo?.voteID ?? null;
        const overwriteCount = voteInfo?.overwriteCount ?? 0;
        const maxVoteOverwrites = election.voteType?.maxVoteOverwrites ?? 0;
        const votesLeft = voteId ? Math.max(0, maxVoteOverwrites - overwriteCount) : maxVoteOverwrites + 1;
        actions.votesLeft(votesLeft);
        setVoted(voteId);
      } catch (e) {
        console.error("error in csp sign info check:", e);
      }
    })();
  }, [state.csp.token, election, loaded.election, setVoted, actions]);
  const vote2 = async (values) => {
    if (!client) {
      throw new Error("no client initialized");
    }
    if (!client.wallet) {
      throw new Error("no signer provided");
    }
    if (!election) {
      throw new Error("no election initialized");
    }
    if (!(election instanceof import_sdk7.PublishedElection)) {
      throw new Error("only published elections can be voted");
    }
    actions.voting();
    setLoading((prev) => ({ ...prev, voting: true }));
    setErrors((prev) => ({ ...prev, voting: null }));
    client.setElectionId(election.id);
    try {
      let vote3 = new import_sdk7.Vote(values);
      if (election.electionType.anonymous && signature) {
        vote3 = new import_sdk7.AnonymousVote(values, signature, password);
      }
      actions.setVote(vote3);
      if (typeof beforeSubmit === "function" && !beforeSubmit(vote3)) {
        return;
      }
      switch (election.census.type) {
        case import_sdk7.CensusType.CSP: {
          const vid = await vote(client, election, state.csp.token, vote3);
          setVoted(vid);
          break;
        }
        case import_sdk7.CensusType.ANONYMOUS:
        case import_sdk7.CensusType.WEIGHTED: {
          const vid = await weightedVote(vote3);
          setVoted(vid);
          break;
        }
        default:
          throw new Error(`unsupported census type "${election.census.type}"`);
      }
      await fetchElection(election.id);
    } catch (e) {
      console.error("there was an error voting:", e);
      setErrors((prev) => ({ ...prev, voting: errorToString(e) }));
      setLoading((prev) => ({ ...prev, voting: false }));
    }
  };
  const weightedVote = async (vote3) => {
    if (!vote3) {
      throw new Error("no vote instance");
    }
    if (![import_sdk7.CensusType.WEIGHTED, import_sdk7.CensusType.ANONYMOUS].includes(election.census?.type)) {
      throw new Error("not a Weighted election");
    }
    return await client.submitVote(vote3);
  };
  const { vote: voteDraft, ...stateRest } = state;
  const isWeighted = (0, import_react8.useMemo)(() => getIsWeighted(election), [election]);
  const wrappedActions = {
    ...actions,
    isAbleToVote: setIsAbleToVote,
    voted: setVoted,
    clear: () => {
      actions.clear();
      setLoading({ census: false, election: false, voting: false });
      setLoaded({ census: false, election: !!data, voted: false });
      setErrors({ census: null, election: null, voting: null });
    },
    error: (error) => {
      setErrors((prev) => ({ ...prev, election: errorToString(error) }));
      setLoading((prev) => ({ ...prev, election: false }));
      setLoaded((prev) => ({ ...prev, election: true }));
    },
    censusError: (error) => {
      actions.voted(null);
      actions.votesLeft(0);
      actions.isAbleToVote(false);
      setErrors((prev) => ({ ...prev, census: errorToString(error) }));
      setLoading((prev) => ({ ...prev, census: false }));
      setLoaded((prev) => ({ ...prev, census: true }));
    },
    votingError: (error) => {
      actions.voted(null);
      setErrors((prev) => ({ ...prev, voting: errorToString(error) }));
      setLoading((prev) => ({ ...prev, voting: false }));
    },
    set: (nextElection) => {
      actions.set(normalizeElection(nextElection));
    },
    load: (id2) => {
      actions.load(id2);
      setLoading((prev) => ({ ...prev, election: true }));
      setErrors((prev) => ({ ...prev, election: null }));
    },
    loadCensus: (voter) => {
      actions.loadCensus(voter);
      setLoading((prev) => ({ ...prev, census: true }));
      setErrors((prev) => ({ ...prev, census: null }));
    },
    voting: () => {
      actions.voting();
      setLoading((prev) => ({ ...prev, voting: true }));
      setErrors((prev) => ({ ...prev, voting: null }));
    }
  };
  return {
    ...rest,
    ...stateRest,
    isWeighted,
    voteDraft,
    loading,
    loaded,
    errors,
    actions: wrappedActions,
    fetchElection,
    localize,
    vote: vote2,
    fetchCensus: censusFetch,
    clearClient: actions.clearClient,
    setClient: actions.setClient,
    sikPassword: actions.sikPassword,
    sikSignature: actions.sikSignature
  };
};

// src/providers/election/ElectionProvider.tsx
var import_jsx_runtime6 = require("react/jsx-runtime");
var ElectionContext = (0, import_react9.createContext)(void 0);
var useElection = () => {
  const ctxt = (0, import_react9.useContext)(ElectionContext);
  if (!ctxt) {
    throw new Error(
      "useElection returned `undefined`, maybe you forgot to wrap the component within <ElectionProvider />?"
    );
  }
  return ctxt;
};
var ElectionProvider = ({ children, ...rest }) => {
  const value = useElectionProvider(rest);
  return /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(ElectionContext.Provider, { value, children });
};
ElectionProvider.displayName = "ElectionProvider";

// src/providers/organization/OrganizationProvider.tsx
var import_react11 = require("react");

// src/providers/organization/use-organization-provider.ts
var import_react_query3 = require("@tanstack/react-query");
var import_sdk9 = require("@vocdoni/sdk");
var import_react10 = require("react");

// src/providers/organization/normalized.ts
var import_sdk8 = require("@vocdoni/sdk");
var normalizeOrganization = (organization) => {
  if (!organization) return void 0;
  const rawAccount = organization.account;
  const accountData = rawAccount instanceof import_sdk8.Account ? rawAccount : new import_sdk8.Account({
    name: rawAccount?.name ?? rawAccount?._name?.default ?? "",
    description: rawAccount?.description ?? rawAccount?._description?.default ?? "",
    header: rawAccount?.header ?? rawAccount?._header ?? "",
    avatar: rawAccount?.avatar ?? rawAccount?._avatar ?? "",
    logo: rawAccount?.logo ?? rawAccount?._logo ?? "",
    feed: rawAccount?.feed ?? rawAccount?._feed?.default ?? "",
    meta: rawAccount?.meta ?? rawAccount?._meta ?? []
  });
  return {
    ...organization,
    account: accountData
  };
};

// src/providers/organization/use-organization-provider.ts
var useOrganizationProvider = ({
  id,
  organization,
  queryOptions,
  organizationQueryOptions
}) => {
  const { client, signer, account: vAccount } = useClient();
  const queryClient = (0, import_react_query3.useQueryClient)();
  const normalizedOrganization = (0, import_react10.useMemo)(() => normalizeOrganization(organization), [organization]);
  const [orgId, setOrgId] = (0, import_react10.useState)(id || normalizedOrganization?.address);
  (0, import_react10.useEffect)(() => {
    if (!id || id === orgId) return;
    setOrgId(id);
  }, [id, orgId]);
  (0, import_react10.useEffect)(() => {
    if (!normalizedOrganization) return;
    if (normalizedOrganization.address === orgId) return;
    setOrgId(normalizedOrganization.address);
    queryClient.setQueryData(queryKeys.organization.byId(normalizedOrganization.address), normalizedOrganization);
  }, [normalizedOrganization, orgId, queryClient]);
  const queryKey = (0, import_react10.useMemo)(() => {
    if (!orgId) return null;
    return queryKeys.organization.byId(orgId);
  }, [orgId]);
  const organizationQuery = (0, import_react_query3.useQuery)({
    queryKey: queryKey || ["organization", "disabled"],
    queryFn: async () => normalizeOrganization(await client.fetchAccount(orgId)),
    enabled: !!orgId,
    initialData: normalizedOrganization,
    ...queryOptions ?? organizationQueryOptions
  });
  const updateMutation = (0, import_react_query3.useMutation)({
    mutationFn: async (account) => {
      let ownerAccount = vAccount;
      if (!ownerAccount) {
        const address = await signer.getAddress();
        ownerAccount = await client.fetchAccount(address);
      }
      if (!(0, import_sdk9.areEqualHexStrings)(await signer.getAddress(), ownerAccount?.address)) {
        throw new Error("You're not the owner of this account");
      }
      return normalizeOrganization(
        await client.updateAccountInfo(account instanceof import_sdk9.Account ? account : new import_sdk9.Account(account))
      );
    },
    onSuccess: (data) => {
      if (data?.address) {
        setOrgId(data.address);
        queryClient.setQueryData(queryKeys.organization.byId(data.address), data);
      }
    }
  });
  const fetch2 = async (id2) => {
    const identifier = id2 || orgId;
    if (!identifier) return;
    if (identifier !== orgId) setOrgId(identifier);
    return queryClient.fetchQuery({
      queryKey: queryKeys.organization.byId(identifier),
      queryFn: async () => normalizeOrganization(await client.fetchAccount(identifier))
    });
  };
  const update = async (account) => {
    return updateMutation.mutateAsync(account);
  };
  const loading = organizationQuery.isFetching || updateMutation.isPending;
  const loaded = organizationQuery.isSuccess || organizationQuery.isError || updateMutation.isSuccess || updateMutation.isError;
  const errors = {
    load: organizationQuery.error ? errorToString(organizationQuery.error) : null,
    update: updateMutation.error ? errorToString(updateMutation.error) : null
  };
  return {
    loading,
    loaded,
    id: orgId,
    errors,
    organization: organizationQuery.data,
    fetch: fetch2,
    update
  };
};

// src/providers/organization/OrganizationProvider.tsx
var import_jsx_runtime7 = require("react/jsx-runtime");
var OrganizationContext = (0, import_react11.createContext)(void 0);
var useOrganization = () => {
  const ctxt = (0, import_react11.useContext)(OrganizationContext);
  if (!ctxt) {
    throw new Error(
      "useOrganization returned `undefined`, maybe you forgot to wrap the component within <OrganizationProvider />?"
    );
  }
  return ctxt;
};
var OrganizationProvider = ({ children, ...rest }) => {
  const value = useOrganizationProvider(rest);
  return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(OrganizationContext.Provider, { value, children });
};
OrganizationProvider.displayName = "OrganizationProvider";

// src/components/Account/Balance.tsx
var import_jsx_runtime8 = require("react/jsx-runtime");
var Balance = (props) => {
  const { balance } = useClient();
  const { AccountBalance: Slot } = useComponents();
  const t = useReactComponentsLocalize();
  if (balance < 0) return null;
  let tone = "success";
  if (balance < 50 && balance > 20) {
    tone = "warning";
  } else if (balance <= 20) {
    tone = "danger";
  }
  const label = t("balance", { balance });
  return /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(Slot, { ...props, balance, tone, label });
};

// src/components/Election/Actions/Actions.tsx
var import_sdk14 = require("@vocdoni/sdk");

// src/components/Election/Actions/ActionsProvider.tsx
var import_jsx_runtime9 = require("react/jsx-runtime");
var ActionsProvider2 = ({ children }) => /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(ActionsProvider, { children });

// src/components/Election/Actions/Cancel.tsx
var import_sdk10 = require("@vocdoni/sdk");

// src/confirm/useConfirm.tsx
var import_react13 = require("react");

// src/confirm/ConfirmProvider.tsx
var import_react12 = require("react");

// src/confirm/ConfirmModal.tsx
var import_jsx_runtime10 = require("react/jsx-runtime");
var ConfirmModal = () => {
  const { prompt, isOpen, cancel } = useConfirm();
  const { ConfirmShell: Shell } = useComponents();
  return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(Shell, { isOpen, onClose: () => cancel?.(), content: prompt });
};

// src/confirm/ConfirmProvider.tsx
var import_jsx_runtime11 = require("react/jsx-runtime");
var useConfirmProvider = () => {
  const [state, setState] = (0, import_react12.useState)({
    prompt: null,
    isOpen: false,
    proceed: null,
    cancel: null
  });
  const confirm = (prompt) => new Promise((resolve, reject) => {
    setState({
      prompt,
      isOpen: true,
      proceed: resolve,
      cancel: reject
    });
  }).then(
    () => {
      setState((prev) => ({ ...prev, isOpen: false }));
      return true;
    },
    () => {
      setState((prev) => ({ ...prev, isOpen: false }));
      return false;
    }
  );
  return { ...state, confirm };
};
var ConfirmContext = (0, import_react12.createContext)(void 0);
var ConfirmProvider = ({ children }) => {
  const value = useConfirmProvider();
  return /* @__PURE__ */ (0, import_jsx_runtime11.jsxs)(ConfirmContext.Provider, { value, children: [
    /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(ConfirmModal, {}),
    children
  ] });
};

// src/confirm/useConfirm.tsx
var useConfirm = () => {
  const context = (0, import_react13.useContext)(ConfirmContext);
  if (!context) {
    throw new Error(
      "useConfirm returned `undefined`, maybe you forgot to wrap the component within <ConfirmProvider />?"
    );
  }
  return context;
};

// src/components/Election/Actions/ConfirmActionModal.tsx
var import_jsx_runtime12 = require("react/jsx-runtime");
var ConfirmActionModal = ({ title, description, confirm, cancel, ...rest }) => {
  const { proceed, cancel: cancelAction } = useConfirm();
  const { ConfirmActionModal: Slot } = useComponents();
  return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
    Slot,
    {
      ...rest,
      title,
      description,
      confirm,
      cancel,
      onConfirm: () => proceed?.(),
      onCancel: () => cancelAction?.()
    }
  );
};

// src/components/Election/Actions/Cancel.tsx
var import_jsx_runtime13 = require("react/jsx-runtime");
var ActionCancel = (props) => {
  const { account } = useClient();
  const localize = useReactComponentsLocalize();
  const { confirm } = useConfirm();
  const { election } = useElection();
  const { ActionCancel: Slot } = useComponents();
  const {
    cancel,
    disabled,
    loading: { cancel: loading }
  } = useActions();
  const handle = async () => {
    if (await confirm(
      /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
        ConfirmActionModal,
        {
          title: localize("confirm.cancel_process_title"),
          description: localize("actions.cancel_description", { election }),
          confirm: localize("confirm.cancel_process_button"),
          cancel: localize("confirm.cancel_button")
        }
      )
    )) {
      await cancel();
    }
  };
  if (!election || !(election instanceof import_sdk10.PublishedElection) || !(0, import_sdk10.areEqualHexStrings)(election.organizationId, account?.address)) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
    Slot,
    {
      ...props,
      loading,
      onClick: handle,
      disabled: disabled || [import_sdk10.ElectionStatus.CANCELED, import_sdk10.ElectionStatus.ENDED, import_sdk10.ElectionStatus.RESULTS].includes(election.status),
      label: localize("actions.cancel")
    }
  );
};

// src/components/Election/Actions/Continue.tsx
var import_sdk11 = require("@vocdoni/sdk");
var import_jsx_runtime14 = require("react/jsx-runtime");
var ActionContinue = (props) => {
  const { account } = useClient();
  const localize = useReactComponentsLocalize();
  const { election } = useElection();
  const { ActionContinue: Slot } = useComponents();
  const {
    resume,
    disabled,
    loading: { continue: loading }
  } = useActions();
  if (!election || !(election instanceof import_sdk11.PublishedElection) || !(0, import_sdk11.areEqualHexStrings)(election.organizationId, account?.address)) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
    Slot,
    {
      ...props,
      loading,
      onClick: resume,
      disabled: disabled || election.status !== import_sdk11.ElectionStatus.PAUSED,
      label: localize("actions.continue")
    }
  );
};

// src/components/Election/Actions/End.tsx
var import_sdk12 = require("@vocdoni/sdk");
var import_jsx_runtime15 = require("react/jsx-runtime");
var ActionEnd = (props) => {
  const { account } = useClient();
  const localize = useReactComponentsLocalize();
  const { confirm } = useConfirm();
  const { election } = useElection();
  const { ActionEnd: Slot } = useComponents();
  const {
    end,
    loading: { end: loading },
    disabled
  } = useActions();
  const handle = async () => {
    if (await confirm(
      /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
        ConfirmActionModal,
        {
          title: localize("confirm.end_process_title"),
          description: localize("actions.end_description", { election }),
          confirm: localize("confirm.end_process_button"),
          cancel: localize("confirm.cancel_button")
        }
      )
    )) {
      await end();
    }
  };
  if (!election || !(election instanceof import_sdk12.PublishedElection) || !(0, import_sdk12.areEqualHexStrings)(election.organizationId, account?.address)) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
    Slot,
    {
      ...props,
      loading,
      onClick: handle,
      disabled: disabled || [import_sdk12.ElectionStatus.RESULTS, import_sdk12.ElectionStatus.ENDED, import_sdk12.ElectionStatus.CANCELED, import_sdk12.ElectionStatus.UPCOMING].includes(
        election.status
      ),
      label: localize("actions.end")
    }
  );
};

// src/components/Election/Actions/Pause.tsx
var import_sdk13 = require("@vocdoni/sdk");
var import_jsx_runtime16 = require("react/jsx-runtime");
var ActionPause = (props) => {
  const { account } = useClient();
  const localize = useReactComponentsLocalize();
  const { election } = useElection();
  const { ActionPause: Slot } = useComponents();
  const {
    pause,
    disabled,
    loading: { pause: loading }
  } = useActions();
  if (!election || !(election instanceof import_sdk13.PublishedElection) || !(0, import_sdk13.areEqualHexStrings)(election.organizationId, account?.address)) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime16.jsx)(
    Slot,
    {
      ...props,
      loading,
      onClick: pause,
      disabled: disabled || election.status !== import_sdk13.ElectionStatus.ONGOING,
      label: localize("actions.pause")
    }
  );
};

// src/components/Election/Actions/Actions.tsx
var import_jsx_runtime17 = require("react/jsx-runtime");
var ElectionActions = (props) => {
  const { account } = useClient();
  const { election } = useElection();
  const { ElectionActions: Slot } = useComponents();
  if (!election || !(election instanceof import_sdk14.PublishedElection) || !(0, import_sdk14.areEqualHexStrings)(election.organizationId, account?.address) || [import_sdk14.ElectionStatus.CANCELED, import_sdk14.ElectionStatus.ENDED, import_sdk14.ElectionStatus.RESULTS].includes(election.status)) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
    Slot,
    {
      ...props,
      actions: /* @__PURE__ */ (0, import_jsx_runtime17.jsxs)(ActionsProvider2, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(ActionContinue, {}),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(ActionPause, {}),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(ActionEnd, {}),
        /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(ActionCancel, {})
      ] })
    }
  );
};

// src/components/Election/Description.tsx
var import_jsx_runtime18 = require("react/jsx-runtime");
var ElectionDescription = (props) => {
  const { election } = useElection();
  const { ElectionDescription: Slot } = useComponents();
  const description = getElectionDescription(election);
  if (!election || !description) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Slot, { ...props, description });
};

// src/components/Election/Header.tsx
var import_sdk15 = require("@vocdoni/sdk");
var import_jsx_runtime19 = require("react/jsx-runtime");
var ElectionHeader = (props) => {
  const { election } = useElection();
  const { ElectionHeader: Slot } = useComponents();
  if (!election || !(election instanceof import_sdk15.PublishedElection)) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(Slot, { ...props, src: linkifyIpfs(election.header), alt: election.title?.default || election.id });
};

// src/components/Election/Questions/Choice.tsx
var import_jsx_runtime20 = require("react/jsx-runtime");
var toNonEmpty = (value) => typeof value === "string" && value.trim().length > 0 ? value : void 0;
var getQuestionChoiceMeta = (choice) => {
  const meta = choice.meta ?? {};
  const imageDefault = toNonEmpty(meta.image?.default);
  const imageThumbnail = toNonEmpty(meta.image?.thumbnail);
  const description = toNonEmpty(meta.description);
  const normalizedDefault = linkifyIpfs(imageDefault);
  const normalizedThumbnail = linkifyIpfs(imageThumbnail);
  const image = normalizedDefault || normalizedThumbnail ? { default: normalizedDefault, thumbnail: normalizedThumbnail } : void 0;
  return {
    image,
    description
  };
};
var hasExtendedChoiceMeta = (choice) => {
  const { image, description } = getQuestionChoiceMeta(choice);
  return Boolean(description || image?.default || image?.thumbnail);
};
var QuestionChoice = ({
  choice,
  value,
  compact,
  dataAttrs,
  selectionMode,
  presentation,
  selected,
  disabled,
  controlType,
  onSelect,
  ...rest
}) => {
  const { QuestionChoice: Slot } = useComponents();
  const metadata = getQuestionChoiceMeta(choice);
  const hasImage = Boolean(metadata.image?.default || metadata.image?.thumbnail);
  const canOpenImageModal = Boolean(metadata.image?.thumbnail && metadata.image?.default);
  return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
    Slot,
    {
      ...rest,
      choice,
      value,
      label: choice.title.default,
      description: metadata.description,
      image: metadata.image,
      compact,
      hasImage,
      canOpenImageModal,
      dataAttrs,
      selectionMode,
      presentation,
      selected,
      disabled,
      controlType,
      onSelect
    }
  );
};

// src/components/Election/Questions/Confirmation.tsx
var import_sdk16 = require("@vocdoni/sdk");
var import_jsx_runtime21 = require("react/jsx-runtime");
var QuestionsConfirmation = ({ answers, election }) => {
  const { QuestionsConfirmation: Slot } = useComponents();
  const { proceed, cancel } = useConfirm();
  const t = useReactComponentsLocalize();
  const answersView = election.questions.map((question, index) => {
    const indexedAnswer = answers[index.toString()];
    const isSingleChoice = election.resultsType.name === import_sdk16.ElectionResultsTypeNames.SINGLE_CHOICE_MULTIQUESTION || typeof indexedAnswer === "string";
    if (isSingleChoice) {
      const selected = parseInt(answers[index.toString()] || "", 10);
      const selectedChoice = question.choices.find((choice) => choice.value === selected);
      return {
        question: question.title.default,
        answers: [selectedChoice?.title.default || ""]
      };
    }
    const rawSelectedValues = answers[0];
    const selectedValues = (Array.isArray(rawSelectedValues) ? rawSelectedValues : ["-1"]).map((value) => Number(value));
    const mappedAnswers = selectedValues.map((value) => question.choices[value]?.title.default || t("vote.abstain"));
    return {
      question: question.title.default,
      answers: mappedAnswers
    };
  });
  return /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
    Slot,
    {
      election,
      answers,
      answersView,
      onConfirm: () => proceed?.(),
      onCancel: () => cancel?.()
    }
  );
};

// src/components/Election/Questions/Empty.tsx
var import_jsx_runtime22 = require("react/jsx-runtime");
var QuestionsEmpty = () => {
  const { QuestionsEmpty: Slot } = useComponents();
  return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Slot, { text: "Apparently this process has no questions \u{1F914}" });
};

// src/components/Election/Questions/Error.tsx
var import_jsx_runtime23 = require("react/jsx-runtime");
var QuestionsError = ({ error }) => {
  const { QuestionsError: Slot } = useComponents();
  return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(Slot, { error });
};

// src/components/Election/Questions/Fields.tsx
var import_sdk19 = require("@vocdoni/sdk");
var import_react_hook_form2 = require("react-hook-form");

// src/components/Election/Questions/Tip.tsx
var import_sdk18 = require("@vocdoni/sdk");

// src/components/Election/Questions/Form.tsx
var import_wallet = require("@ethersproject/wallet");
var import_sdk17 = require("@vocdoni/sdk");
var import_react14 = require("react");
var import_react_hook_form = require("react-hook-form");
var import_jsx_runtime24 = require("react/jsx-runtime");
var QuestionsFormContext = (0, import_react14.createContext)(void 0);
var useQuestionsForm = () => {
  const context = (0, import_react14.useContext)(QuestionsFormContext);
  if (!context) {
    throw new Error("useQuestionsForm must be used within a QuestionsFormProvider");
  }
  return context;
};
var QuestionsFormProvider = ({ children }) => {
  const fmethods = (0, import_react_hook_form.useForm)();
  const { confirm } = useConfirm();
  const { election, client, vote: baseVote, connected } = useElection();
  const vote2 = async (values) => {
    if (!election || !(election instanceof import_sdk17.PublishedElection)) {
      console.warn("vote attempt with no valid election defined");
      return false;
    }
    if (client.wallet instanceof import_wallet.Wallet && !await confirm(/* @__PURE__ */ (0, import_jsx_runtime24.jsx)(QuestionsConfirmation, { election, answers: values }))) {
      return false;
    }
    let results2 = [];
    switch (election.resultsType.name) {
      case import_sdk17.ElectionResultsTypeNames.SINGLE_CHOICE_MULTIQUESTION:
        results2 = election.questions.map((_question, index) => parseInt(values[index.toString()], 10));
        break;
      case import_sdk17.ElectionResultsTypeNames.MULTIPLE_CHOICE:
        results2 = (Object.values(values).pop() || []).map((value) => parseInt(value, 10));
        if (results2.includes(-1) || election.resultsType.properties.canAbstain && (!values || results2.length < election.voteType.maxCount)) {
          if (results2.includes(-1)) {
            results2.splice(results2.indexOf(-1), 1);
          }
          let abstainIndex = 0;
          while (results2.length < (election.voteType.maxCount || 1)) {
            results2.push(parseInt(election.resultsType.properties.abstainValues[abstainIndex++], 10));
          }
        }
        break;
      case import_sdk17.ElectionResultsTypeNames.APPROVAL:
        results2 = election.questions[0].choices.map((_choice, index) => values[0].includes(index.toString()) ? 1 : 0);
        break;
      default:
        throw new Error("Unknown or invalid election type");
    }
    return baseVote(results2);
  };
  (0, import_react14.useEffect)(() => {
    if (connected || !election || !(election instanceof import_sdk17.PublishedElection) || !election.questions) return;
    fmethods.reset({
      ...election.questions.reduce((acc, _question, index) => ({ ...acc, [index]: "" }), {})
    });
  }, [election, fmethods, connected]);
  return /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(import_react_hook_form.FormProvider, { ...fmethods, children: /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(QuestionsFormContext.Provider, { value: { fmethods, vote: vote2 }, children }) });
};

// src/components/Election/Questions/Tip.tsx
var import_jsx_runtime25 = require("react/jsx-runtime");
var QuestionTip = () => {
  const { QuestionTip: Slot } = useComponents();
  const {
    fmethods: { getValues }
  } = useQuestionsForm();
  const { election } = useElection();
  const t = useReactComponentsLocalize();
  if (!election || !(election instanceof import_sdk18.PublishedElection)) return null;
  let text = "";
  if (election.resultsType.name === import_sdk18.ElectionResultsTypeNames.MULTIPLE_CHOICE) {
    text = t("question_types.multichoice_desc", {
      selected: getValues()[0]?.length,
      maxcount: election.voteType.maxCount
    });
    if (election.resultsType.properties.canAbstain) {
      text += t("question_types.multichoice_desc_abstain");
    }
  }
  if (!text) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Slot, { text });
};

// src/components/Election/Questions/Fields.tsx
var import_jsx_runtime26 = require("react/jsx-runtime");
var getSelectionMode = (resultsType) => resultsType === import_sdk19.ElectionResultsTypeNames.SINGLE_CHOICE_MULTIQUESTION ? "single" : "multiple";
var getQuestionPresentation = (question) => question.choices.some(hasExtendedChoiceMeta) ? "extended" : "basic";
var getQuestionLayout = (question) => question.choices.some((choice) => Boolean(choice.meta?.image?.default)) ? "grid" : "list";
var ElectionQuestion = ({ question, index }) => {
  const { election } = useElection();
  const { ElectionQuestion: Slot } = useComponents();
  const {
    formState: { errors }
  } = (0, import_react_hook_form2.useFormContext)();
  const layout = getQuestionLayout(question);
  const hasExtendedChoices = question.choices.some(hasExtendedChoiceMeta);
  const selectionMode = election instanceof import_sdk19.PublishedElection ? getSelectionMode(election.resultsType.name) : "single";
  const invalid = Boolean(errors[index]);
  return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
    Slot,
    {
      question,
      index,
      layout,
      invalid,
      hasExtendedChoices,
      selectionMode,
      title: question.title.default,
      description: question.description?.default,
      fields: /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
        FieldSwitcher,
        {
          question,
          index,
          layout,
          presentation: getQuestionPresentation(question)
        }
      ),
      tip: /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(QuestionTip, {})
    }
  );
};
var FieldSwitcher = (props) => {
  const { election } = useElection();
  if (!(election instanceof import_sdk19.PublishedElection)) return null;
  switch (election.resultsType.name) {
    case import_sdk19.ElectionResultsTypeNames.MULTIPLE_CHOICE:
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(MultiChoice, { ...props });
    case import_sdk19.ElectionResultsTypeNames.APPROVAL:
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(ApprovalChoice, { ...props });
    case import_sdk19.ElectionResultsTypeNames.SINGLE_CHOICE_MULTIQUESTION:
    default:
      return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(SingleChoice, { ...props });
  }
};
var MultiChoice = ({
  index,
  question,
  layout,
  presentation
}) => {
  const {
    election,
    isAbleToVote: isAbleToVote2,
    loading: { voting }
  } = useElection();
  const t = useReactComponentsLocalize();
  const { control, trigger } = (0, import_react_hook_form2.useFormContext)();
  const { QuestionsError: QuestionsError2 } = useComponents();
  if (!(election instanceof import_sdk19.PublishedElection) || election.resultsType.name !== import_sdk19.ElectionResultsTypeNames.MULTIPLE_CHOICE)
    return null;
  const disabled = election.status !== import_sdk19.ElectionStatus.ONGOING || !isAbleToVote2 || voting;
  const choices = [...question.choices];
  const canAbstain = election.resultsType.properties.canAbstain;
  const shouldRenderAbstain = canAbstain && !election.get("questions.hideAbstain");
  if (canAbstain && shouldRenderAbstain) {
    choices.push({ title: { default: t("vote.abstain") }, value: -1 });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
    import_react_hook_form2.Controller,
    {
      control,
      disabled,
      name: index,
      rules: {
        validate: (value) => {
          if (!shouldRenderAbstain || value && value.includes("-1") && value.length < election.voteType.maxCount) {
            return true;
          }
          return value && value.length === election.voteType.maxCount || t("validation.choices_count", { count: election.voteType.maxCount });
        }
      },
      render: ({ field, fieldState }) => {
        const currentValues = Array.isArray(field.value) ? field.value : [];
        return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(import_jsx_runtime26.Fragment, { children: [
          choices.map((choice) => {
            const value = choice.value.toString();
            const maxSelected = currentValues.length >= election.voteType.maxCount && !currentValues.includes(value);
            return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
              QuestionChoice,
              {
                choice,
                value,
                controlType: "checkbox",
                selectionMode: "multiple",
                presentation,
                compact: !Boolean(choice.meta?.image?.default) && layout === "list",
                dataAttrs: {
                  "data-choice-card": "",
                  "data-choice-control": "",
                  "data-choice-body": "",
                  "data-choice-media": "",
                  "data-layout": layout,
                  "data-choice-id-base": `question-${index}-choice-${value}`,
                  "data-choice-field-name": field.name
                },
                selected: currentValues.includes(value),
                disabled: disabled || maxSelected,
                onSelect: (checked) => {
                  if (checked && maxSelected) return;
                  const next = checked ? [...currentValues, value] : currentValues.filter((currentValue) => currentValue !== value);
                  field.onChange(next);
                  trigger(index);
                }
              },
              value
            );
          }),
          fieldState.error?.message ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(QuestionsError2, { error: fieldState.error.message, variant: "field" }) : null
        ] });
      }
    }
  );
};
var ApprovalChoice = ({
  index,
  question,
  layout,
  presentation
}) => {
  const {
    election,
    isAbleToVote: isAbleToVote2,
    loading: { voting }
  } = useElection();
  const { control } = (0, import_react_hook_form2.useFormContext)();
  const { QuestionsError: QuestionsError2 } = useComponents();
  const t = useReactComponentsLocalize();
  if (!(election instanceof import_sdk19.PublishedElection) || election.resultsType.name !== import_sdk19.ElectionResultsTypeNames.APPROVAL)
    return null;
  const disabled = election.status !== import_sdk19.ElectionStatus.ONGOING || !isAbleToVote2 || voting;
  return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
    import_react_hook_form2.Controller,
    {
      control,
      disabled,
      name: index,
      rules: {
        validate: (value) => value && value.length > 0 || t("validation.at_least_one", { defaultValue: "Select at least one option" })
      },
      render: ({ field, fieldState }) => {
        const currentValues = Array.isArray(field.value) ? field.value : [];
        return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(import_jsx_runtime26.Fragment, { children: [
          question.choices.map((choice) => {
            const value = choice.value.toString();
            return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
              QuestionChoice,
              {
                choice,
                value,
                controlType: "checkbox",
                selectionMode: "multiple",
                presentation,
                compact: !Boolean(choice.meta?.image?.default) && layout === "list",
                dataAttrs: {
                  "data-choice-card": "",
                  "data-choice-control": "",
                  "data-choice-body": "",
                  "data-choice-media": "",
                  "data-layout": layout,
                  "data-choice-id-base": `question-${index}-choice-${value}`,
                  "data-choice-field-name": field.name
                },
                selected: currentValues.includes(value),
                disabled,
                onSelect: (checked) => {
                  const next = checked ? [...currentValues, value] : currentValues.filter((currentValue) => currentValue !== value);
                  field.onChange(next);
                }
              },
              value
            );
          }),
          fieldState.error?.message ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(QuestionsError2, { error: fieldState.error.message, variant: "field" }) : null
        ] });
      }
    }
  );
};
var SingleChoice = ({
  index,
  question,
  layout,
  presentation
}) => {
  const {
    election,
    isAbleToVote: isAbleToVote2,
    loading: { voting }
  } = useElection();
  const { control } = (0, import_react_hook_form2.useFormContext)();
  const { QuestionsError: QuestionsError2 } = useComponents();
  const t = useReactComponentsLocalize();
  if (!(election instanceof import_sdk19.PublishedElection)) return null;
  const disabled = election.status !== import_sdk19.ElectionStatus.ONGOING || !isAbleToVote2 || voting;
  return /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
    import_react_hook_form2.Controller,
    {
      control,
      disabled,
      name: index,
      rules: { required: t("validation.required") },
      render: ({ field, fieldState }) => /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(import_jsx_runtime26.Fragment, { children: [
        question.choices.map((choice) => /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(
          QuestionChoice,
          {
            choice,
            value: choice.value.toString(),
            controlType: "radio",
            selectionMode: "single",
            presentation,
            compact: !Boolean(choice.meta?.image?.default) && layout === "list",
            dataAttrs: {
              "data-choice-card": "",
              "data-choice-control": "",
              "data-choice-body": "",
              "data-choice-media": "",
              "data-layout": layout
            },
            selected: field.value === choice.value.toString(),
            disabled,
            onSelect: (checked) => {
              if (!checked) return;
              field.onChange(choice.value.toString());
            }
          },
          choice.value
        )),
        fieldState.error?.message ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(QuestionsError2, { error: fieldState.error.message, variant: "field" }) : null
      ] })
    }
  );
};

// src/components/Election/Questions/Questions.tsx
var import_sdk20 = require("@vocdoni/sdk");

// src/environment.ts
var environment = {
  explorer: (env) => {
    if (env === "dev") {
      return "https://dev.explorer.vote";
    }
    return "https://explorer.vote";
  },
  verifyVote: (env, proof) => `${environment.explorer(env)}/verify/#/${proof}`
};

// src/components/Election/Questions/Voted.tsx
var import_jsx_runtime27 = require("react/jsx-runtime");
var Voted = () => {
  const { env } = useClient();
  const { voted } = useElection();
  const { Voted: Slot } = useComponents();
  const t = useReactComponentsLocalize();
  if (!voted) return null;
  const description = t("vote.voted_description", { id: voted });
  const parts = description.split(voted);
  const descriptionContent = parts.length > 1 ? parts.reduce((acc, part, index) => {
    acc.push(part);
    if (index < parts.length - 1) {
      acc.push(
        /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("a", { href: environment.verifyVote(env, voted), target: "_blank", rel: "noreferrer", children: voted }, `link-${index}`)
      );
    }
    return acc;
  }, []) : description;
  return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(Slot, { title: t("vote.voted_title"), description: descriptionContent });
};

// src/components/Election/Questions/Questions.tsx
var import_jsx_runtime28 = require("react/jsx-runtime");
var ElectionQuestions = (props) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(QuestionsFormProvider, { children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ElectionQuestionsForm, { ...props }) });
var ElectionQuestionsForm = ({ onInvalid, ...rest }) => {
  const { election } = useElection();
  const { ElectionQuestions: Slot } = useComponents();
  if (!(election instanceof import_sdk20.PublishedElection)) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Slot, { ...rest, form: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(QuestionsFormContents, { onInvalid }) });
};
var QuestionsFormContents = ({ onInvalid }) => {
  const {
    election,
    voted,
    errors: { voting: error },
    isAbleToVote: isAbleToVote2
  } = useElection();
  const { QuestionsEmpty: QuestionsEmpty2, QuestionsError: QuestionsError2 } = useComponents();
  const t = useReactComponentsLocalize();
  const { fmethods, vote: vote2 } = useQuestionsForm();
  const questions = election?.questions;
  if (voted && !isAbleToVote2) {
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Voted, {});
  }
  if (!questions || !questions.length) {
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(QuestionsEmpty2, { text: t("empty") });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("form", { onSubmit: fmethods.handleSubmit(vote2, onInvalid), id: `election-questions-${election.id}`, children: [
    /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(Voted, {}),
    questions.map((question, index) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ElectionQuestion, { index: index.toString(), question }, index)),
    error ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(QuestionsError2, { error, variant: "form" }) : null
  ] });
};

// src/components/Election/Questions/TypeBadge.tsx
var import_sdk21 = require("@vocdoni/sdk");
var import_jsx_runtime29 = require("react/jsx-runtime");
var QuestionsTypeBadge = (props) => {
  const { election, isWeighted } = useElection();
  const { QuestionsTypeBadge: Slot } = useComponents();
  const t = useReactComponentsLocalize();
  if (!election || !(election instanceof import_sdk21.PublishedElection) || !election.census) {
    return null;
  }
  const weighted = isWeighted ? t("question_types.weighted_voting") : "";
  let title = "";
  let tooltip = "";
  switch (election.resultsType.name) {
    case import_sdk21.ElectionResultsTypeNames.SINGLE_CHOICE_MULTIQUESTION:
      title = t("question_types.singlechoice_title", { weighted });
      break;
    case import_sdk21.ElectionResultsTypeNames.MULTIPLE_CHOICE:
      title = t("question_types.multichoice_title", { weighted });
      tooltip = t("question_types.multichoice_tooltip", { maxcount: election.voteType.maxCount });
      break;
    case import_sdk21.ElectionResultsTypeNames.APPROVAL:
      title = t("question_types.approval_title");
      tooltip = t("question_types.approval_tooltip", { maxcount: election.voteType.maxCount });
      break;
    default:
      return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(Slot, { ...props, title, tooltip });
};

// src/components/Election/Results.tsx
var import_sdk22 = require("@vocdoni/sdk");
var import_date_fns = require("date-fns");
var import_jsx_runtime30 = require("react/jsx-runtime");
var percent = (result, total) => (Number(result) / total * 100 || 0).toFixed(1) + "%";
var results = (result, decimals) => decimals ? parseInt((0, import_sdk22.formatUnits)(BigInt(result), decimals), 10) : result;
var electionChoices = (election, q, abstainLabel) => {
  const nchoices = [...q.choices];
  if (election.resultsType.name === import_sdk22.ElectionResultsTypeNames.MULTIPLE_CHOICE && election.resultsType.properties.canAbstain) {
    const abstain = {
      title: { default: abstainLabel },
      results: q.numAbstains,
      value: -1
    };
    nchoices.push(abstain);
  }
  return nchoices;
};
var ElectionResults = ({ extended = false, forceRender, ...rest }) => {
  const { election } = useElection();
  const localize = useReactComponentsLocalize();
  const { ElectionResults: Slot } = useComponents();
  if (!election || !(election instanceof import_sdk22.PublishedElection) || election.status === import_sdk22.ElectionStatus.CANCELED) return null;
  if (election.electionType.secretUntilTheEnd && election.status !== import_sdk22.ElectionStatus.RESULTS && !forceRender) {
    return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
      Slot,
      {
        ...rest,
        secretText: localize("results.secret_until_the_end", {
          endDate: (0, import_date_fns.format)(election.endDate, localize("results.date_format"))
        })
      }
    );
  }
  const decimals = election.meta?.token?.decimals || 0;
  const totalsAbstain = election.questions.map((q) => "numAbstains" in q ? Number(q.numAbstains) : 0);
  const totals = election.questions.map((el, idx) => el.choices.reduce((acc, curr) => acc + Number(curr.results), totalsAbstain[idx])).map((votes) => results(votes, decimals));
  const questions = election.questions.map((question, idx) => {
    const choices = electionChoices(election, question, localize("vote.abstain"));
    return {
      title: localize("results.title", { title: question.title.default }),
      choices: choices.map((choice) => {
        const meta = choice.meta ?? {};
        const description = (0, import_sdk22.dotobject)(meta, "description");
        const image = (0, import_sdk22.dotobject)(meta, "image.default");
        const votes = results(choice.results ?? 0, decimals);
        return {
          title: choice.title.default,
          votes: String(votes),
          percent: percent(votes, totals[idx]),
          description: extended ? description : void 0,
          image: extended ? linkifyIpfs(image) : void 0
        };
      })
    };
  });
  return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(Slot, { ...rest, questions });
};

// src/components/Election/Schedule.tsx
var import_sdk23 = require("@vocdoni/sdk");
var import_date_fns2 = require("date-fns");
var import_react15 = require("react");
var import_jsx_runtime31 = require("react/jsx-runtime");
var subscribe = () => () => {
};
var useIsHydrationRender = () => (0, import_react15.useSyncExternalStore)(
  subscribe,
  () => false,
  () => true
);
var formatDeterministicDate = (date) => date.toISOString();
var ElectionSchedule = ({
  format: format2 = "PPp",
  showRemaining = false,
  showCreatedAt = false,
  ...rest
}) => {
  const { election } = useElection();
  const t = useReactComponentsLocalize();
  const { ElectionSchedule: Slot } = useComponents();
  const isHydrationRender = useIsHydrationRender();
  const startDate = getElectionDate(election, "startDate");
  const endDate = getElectionDate(election, "endDate");
  const creationTime = getElectionDate(election, "creationTime");
  const status = getElectionStatus(election);
  if (!election || !startDate || !endDate || !status) return null;
  const getRemaining = (now) => {
    switch (status) {
      case import_sdk23.ElectionStatus.ONGOING:
      case import_sdk23.ElectionStatus.RESULTS:
        if (endDate < now) {
          return t("schedule.ended", {
            distance: (0, import_date_fns2.formatDistance)(endDate, now, { addSuffix: true })
          });
        }
        return (0, import_date_fns2.formatDistance)(endDate, now, { addSuffix: true });
      case import_sdk23.ElectionStatus.ENDED:
        return t("schedule.ended", {
          distance: (0, import_date_fns2.formatDistance)(endDate, now, { addSuffix: true })
        });
      case import_sdk23.ElectionStatus.PAUSED:
        if (now < startDate) {
          return t("schedule.paused_start", {
            distance: (0, import_date_fns2.formatDistance)(startDate, now, { addSuffix: true })
          });
        }
        return t("schedule.paused_end", {
          distance: (0, import_date_fns2.formatDistance)(endDate, now, { addSuffix: true })
        });
      case import_sdk23.ElectionStatus.UPCOMING:
      default:
        return (0, import_date_fns2.formatDistance)(startDate, now, { addSuffix: true });
    }
  };
  const getDeterministicText = () => {
    if (showCreatedAt && creationTime) {
      return t("schedule.created", {
        distance: formatDeterministicDate(creationTime)
      });
    }
    return t("schedule.from_begin_to_end", {
      begin: formatDeterministicDate(startDate),
      end: formatDeterministicDate(endDate)
    });
  };
  let text = getDeterministicText();
  if (!isHydrationRender) {
    const now = /* @__PURE__ */ new Date();
    text = t("schedule.from_begin_to_end", {
      begin: (0, import_date_fns2.format)(startDate, format2),
      end: (0, import_date_fns2.format)(endDate, format2)
    });
    if (showRemaining) {
      text = getRemaining(now);
    } else if (showCreatedAt && creationTime) {
      text = t("schedule.created", {
        distance: (0, import_date_fns2.formatDistance)(creationTime, now, { addSuffix: true })
      });
    }
  }
  return /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(Slot, { ...rest, text });
};

// src/components/Election/SpreadsheetAccess.tsx
var import_wallet2 = require("@ethersproject/wallet");
var import_sdk24 = require("@vocdoni/sdk");
var import_react16 = require("react");
var import_react_hook_form3 = require("react-hook-form");
var import_jsx_runtime32 = require("react/jsx-runtime");
var SpreadsheetAccess = ({ hashPrivateKey, className }) => {
  const [open, setOpen] = (0, import_react16.useState)(false);
  const [loading, setLoading] = (0, import_react16.useState)(false);
  const [formError, setFormError] = (0, import_react16.useState)();
  const [privkey, setPrivkey] = (0, import_react16.useState)("");
  const { SpreadsheetAccess: Slot } = useComponents();
  const localize = useReactComponentsLocalize();
  const { env } = useClient();
  const { connected, clearClient, election, client: cl, setClient, sikPassword, sikSignature } = useElection();
  const {
    register,
    handleSubmit,
    formState: { errors },
    reset
  } = (0, import_react_hook_form3.useForm)();
  const shouldRender = isPublishedElectionLike(election) && getElectionField(election, "census.type") === "spreadsheet";
  (0, import_react16.useEffect)(() => {
    setPrivkey(getLocationHash());
  }, []);
  (0, import_react16.useEffect)(() => {
    ;
    (async () => {
      try {
        if (!shouldRender || !privkey) return;
        const privKeyWallet = new import_wallet2.Wallet(privkey);
        const client = new import_sdk24.VocdoniSDKClient({
          env,
          wallet: privKeyWallet,
          electionId: election?.id,
          api_url: cl.chainService.url
        });
        setClient(client);
      } catch (error) {
        console.warn("Error trying to login with private key ", error);
        setClient(cl);
      }
    })();
  }, [election, env, shouldRender, privkey, setClient, cl]);
  if (!shouldRender) return null;
  const required = { value: true, message: localize("validation.required") };
  const minLength = { value: 8, message: localize("validation.min_length", { min: 8 }) };
  const specs = election.get("census.specs");
  const fields = election.get("census.fields");
  const fspecs = (field) => {
    if (!specs || typeof specs[field] === "undefined") return;
    const spec = specs[field];
    const rules = {};
    if (spec.pattern) {
      rules.pattern = {
        value: new RegExp(spec.pattern.value),
        message: spec.pattern.message
      };
    }
    if (spec.props) {
      if (typeof spec.props.max !== "undefined") rules.max = spec.props.max;
      if (typeof spec.props.min !== "undefined") rules.min = spec.props.min;
    }
    return rules;
  };
  const ispecs = (field) => {
    if (!specs || typeof specs[field] === "undefined") return;
    const spec = specs[field];
    const attrs = {};
    if (spec.type) attrs.type = spec.type;
    if (spec.props) {
      if (typeof spec.props.max !== "undefined") attrs.max = spec.props.max;
      if (typeof spec.props.min !== "undefined") attrs.min = spec.props.min;
    }
    return attrs;
  };
  const description = (field) => {
    if (!specs || typeof specs[field] === "undefined") return;
    return specs[field].description;
  };
  const onSubmit = async (vals) => {
    try {
      setLoading(true);
      setFormError(void 0);
      let sikp = null;
      if (election.electionType.anonymous) {
        sikp = vals.sik_password;
        delete vals.sik_password;
      }
      const row = fields.map((_, index) => `${vals[index.toString()] ?? ""}`);
      const hid = await cl.electionService.getNumericElectionId(election.id);
      const salt = election.get("census.salt") || await cl.electionService.getElectionSalt(election.organizationId, hid);
      const wallet = walletFromRow(salt, row);
      const client = new import_sdk24.VocdoniSDKClient({
        env,
        wallet,
        electionId: election.id,
        api_url: cl.chainService.url
      });
      const inCensus = await client.isInCensus();
      if (!inCensus) {
        setFormError(localize("errors.wrong_data_description"));
        return;
      }
      if (election.electionType.anonymous && sikp) {
        const signature = await client.anonymousService.signSIKPayload(wallet);
        const sik = await client.anonymousService.fetchAccountSIK(wallet.address).catch(() => false);
        const valid = await client.anonymousService.hasRegisteredSIK(wallet.address, signature, sikp);
        if (sik && !valid) {
          setFormError(localize("errors.wrong_data_description"));
          return;
        }
        sikPassword(sikp);
        sikSignature(signature);
      }
      setClient(client);
      if (hashPrivateKey) {
        replaceLocationHash(wallet.privateKey);
      }
      reset();
      setOpen(false);
    } catch (error) {
      setFormError(errorToString(error));
    } finally {
      setLoading(false);
    }
  };
  const logout = () => {
    setPrivkey("");
    clearLocationHash();
    clearClient();
  };
  return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
    Slot,
    {
      className,
      connected,
      loading,
      formError,
      title: localize("spreadsheet.modal_title"),
      open,
      onOpen: () => {
        setFormError(void 0);
        setOpen(true);
      },
      onClose: () => {
        if (loading) return;
        setFormError(void 0);
        setOpen(false);
      },
      onLogout: logout,
      onSubmit: handleSubmit(onSubmit),
      fields: fields.map((field, key) => ({
        id: key.toString(),
        label: field,
        description: description(field),
        error: errors[key]?.message?.toString(),
        inputProps: register(key.toString(), {
          required,
          ...fspecs(field)
        }),
        inputAttrs: ispecs(field)
      })),
      anonymousField: election.electionType.anonymous ? {
        id: "sik_password",
        label: localize("spreadsheet.anon_sik_label"),
        description: localize("spreadsheet.anon_sik_helper"),
        error: errors.sik_password?.message?.toString(),
        inputProps: register("sik_password", { required, minLength }),
        inputAttrs: { type: "password" }
      } : void 0
    }
  );
};

// src/components/Election/StatusBadge.tsx
var import_sdk25 = require("@vocdoni/sdk");
var import_jsx_runtime33 = require("react/jsx-runtime");
var ElectionStatusBadge = (props) => {
  const { election } = useElection();
  const localize = useReactComponentsLocalize();
  const { ElectionStatusBadge: Slot } = useComponents();
  if (!election) return null;
  let tone = "success";
  const status = getElectionStatus(election);
  if (status && [import_sdk25.ElectionStatus.PAUSED, import_sdk25.ElectionStatus.ENDED].includes(status)) {
    tone = "warning";
  }
  if (isInvalidElectionLike(election) || status && [import_sdk25.ElectionStatus.CANCELED, import_sdk25.ElectionStatus.PROCESS_UNKNOWN].includes(status)) {
    tone = "danger";
  }
  const label = isInvalidElectionLike(election) ? localize("statuses.invalid") : status ? localize(`statuses.${status.toLowerCase()}`) : localize("statuses.invalid");
  return /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(Slot, { ...props, tone, label });
};

// src/components/Election/Title.tsx
var import_jsx_runtime34 = require("react/jsx-runtime");
var ElectionTitle = (props) => {
  const { election } = useElection();
  const { ElectionTitle: Slot } = useComponents();
  if (!election) return null;
  const title = getElectionTitle(election);
  return /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(Slot, { ...props, title });
};

// src/components/Election/VoteButton.tsx
var import_sdk26 = require("@vocdoni/sdk");
var import_react17 = require("react");
var import_jsx_runtime35 = require("react/jsx-runtime");
var VoteButton = (props) => {
  const externalDisabled = Boolean(props.disabled);
  const { connected } = useClient();
  const {
    client,
    connected: electionConnected,
    loading: { voting },
    ConnectButton,
    isAbleToVote: isAbleToVote2,
    election,
    voted,
    sik: { signature },
    sikSignature
  } = useElection();
  const { VoteButton: Slot } = useComponents();
  const t = useReactComponentsLocalize();
  const [loading, setLoading] = (0, import_react17.useState)(false);
  if (!election || election instanceof import_sdk26.InvalidElection) {
    return null;
  }
  const isDisabled = !client.wallet || !isAbleToVote2 || election.status !== import_sdk26.ElectionStatus.ONGOING || externalDisabled;
  if (!connected && !electionConnected && ConnectButton) {
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(ConnectButton, {});
  }
  const button = {
    type: "submit",
    ...props,
    form: `election-questions-${election.id}`,
    disabled: isDisabled,
    loading: voting,
    label: voted && isAbleToVote2 ? t("vote.button_update") : t("vote.button")
  };
  if (connected && election.electionType.anonymous && !signature) {
    button.loading = loading;
    button.type = "button";
    button.disabled = !client.wallet || !isAbleToVote2;
    button.label = t("vote.sign");
    button.onClick = async () => {
      setLoading(true);
      try {
        sikSignature(await client.anonymousService.signSIKPayload(client.wallet));
      } finally {
        setLoading(false);
      }
    };
  }
  if ([import_sdk26.ElectionStatus.ENDED, import_sdk26.ElectionStatus.RESULTS].includes(election.status) && !voted && signature) {
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("p", { children: t("errors.not_voted_in_ended_election") });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(Slot, { ...button });
};

// src/components/Election/Election.tsx
var import_jsx_runtime36 = require("react/jsx-runtime");
var Election = (props) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionProvider, { ...props, fetchCensus: true, children: /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionBody, {}) });
var ElectionBody = () => {
  const {
    errors: { election: error },
    election,
    connected
  } = useElection();
  const { HR: HR2 } = useComponents();
  if (error) {
    return /* @__PURE__ */ (0, import_jsx_runtime36.jsx)("p", { children: error });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)(import_jsx_runtime36.Fragment, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionHeader, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionTitle, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionSchedule, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionStatusBadge, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionActions, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionDescription, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(HR2, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionQuestions, {}),
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(VoteButton, {}),
    isPublishedElectionLike(election) && getElectionField(election, "census.type") === "spreadsheet" && connected ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(SpreadsheetAccess, {}) : null,
    /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(ElectionResults, {})
  ] });
};

// src/components/Election/Envelope.tsx
var Envelope = () => null;

// src/components/Election/VoteWeight.tsx
var import_sdk27 = require("@vocdoni/sdk");
var import_react18 = require("react");
var import_up_fetch2 = require("up-fetch");
var import_jsx_runtime37 = require("react/jsx-runtime");
var upfetch = (0, import_up_fetch2.up)(fetch);
var VoteWeight = () => {
  const {
    client,
    election,
    csp: { token }
  } = useElection();
  const localize = useReactComponentsLocalize();
  const { VoteWeight: Slot } = useComponents();
  const [weight, setWeight] = (0, import_react18.useState)(null);
  (0, import_react18.useEffect)(() => {
    ;
    (async () => {
      try {
        if (!client || !election || !client.wallet || !(election instanceof import_sdk27.PublishedElection) || !election.census.censusId || election.census.type === import_sdk27.CensusType.CSP && !token) {
          return;
        }
        if (election.census.type !== import_sdk27.CensusType.CSP) {
          const proof = await client.fetchProof(election.census.censusId, await client.wallet.getAddress());
          const decimals = election.meta?.token?.decimals || 0;
          setWeight(results(Number(proof.weight), decimals));
        } else {
          const { weight: weight2 } = await upfetch(`${election.census.censusURI}/weight`, {
            method: "POST",
            body: { authToken: token }
          });
          setWeight(results(Number.parseInt(weight2, 16)));
        }
      } catch (error) {
        console.warn("Error fetching voter weight", error);
        setWeight(null);
      }
    })();
  }, [client, election, token]);
  if (!weight || !election || !(election instanceof import_sdk27.PublishedElection)) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(Slot, { label: localize("vote.weight"), weight });
};

// src/components/Organization/Avatar.tsx
var import_jsx_runtime38 = require("react/jsx-runtime");
var OrganizationAvatar = (props) => {
  const { organization } = useOrganization();
  const { OrganizationAvatar: Slot } = useComponents();
  let avatar = organization?.account.avatar;
  if (!avatar) {
    avatar = organization?.account.logo;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Slot, { ...props, src: linkifyIpfs(avatar), alt: organization?.account.name.default });
};
var OrganizationImage = OrganizationAvatar;

// src/components/Organization/Description.tsx
var import_jsx_runtime39 = require("react/jsx-runtime");
var OrganizationDescription = (props) => {
  const { organization } = useOrganization();
  const { OrganizationDescription: Slot } = useComponents();
  if (!organization?.account.description?.default) return null;
  return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(Slot, { ...props, description: organization.account.description.default });
};

// src/components/Organization/Header.tsx
var import_jsx_runtime40 = require("react/jsx-runtime");
var OrganizationHeader = (props) => {
  const { organization } = useOrganization();
  const { OrganizationAvatar: Slot } = useComponents();
  let avatar = organization?.account.avatar;
  if (!avatar) {
    avatar = organization?.account.logo;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(Slot, { ...props, src: avatar, alt: organization?.account.name.default });
};

// src/components/Organization/Name.tsx
var import_jsx_runtime41 = require("react/jsx-runtime");
var OrganizationName = (props) => {
  const { organization } = useOrganization();
  const { OrganizationName: Slot } = useComponents();
  if (!organization) return null;
  const name = organization.account.name.default || organization.address;
  return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(Slot, { ...props, name });
};

// src/client.tsx
var import_jsx_runtime42 = require("react/jsx-runtime");
var ClientProvider2 = ({ children, ...props }) => {
  return /* @__PURE__ */ (0, import_jsx_runtime42.jsx)(ClientProvider, { ...props, children: /* @__PURE__ */ (0, import_jsx_runtime42.jsx)(ConfirmProvider, { children }) });
};

// src/components/context/helpers.ts
var composeComponents = (...partials) => partials.reduce((acc, partial) => ({ ...acc, ...partial || {} }), {});
var defineComponent = (component) => {
  return component;
};

// src/providers/pagination/PaginationProvider.tsx
var import_react19 = require("react");
var import_jsx_runtime43 = require("react/jsx-runtime");
var PaginationContext = (0, import_react19.createContext)(void 0);
var usePagination = () => {
  const context = (0, import_react19.useContext)(PaginationContext);
  if (!context) {
    throw new Error("usePagination must be used within a PaginationProvider");
  }
  return context;
};
var PaginationProvider = ({
  pagination,
  initialPage = 0,
  ...rest
}) => {
  const [page, setPage] = (0, import_react19.useState)(initialPage);
  return /* @__PURE__ */ (0, import_jsx_runtime43.jsx)(PaginationContext.Provider, { value: { page, setPage, pagination, initialPage }, ...rest });
};

// src/components/Pagination/Button.tsx
var import_jsx_runtime44 = require("react/jsx-runtime");
var Button = ({ isActive, ...props }) => {
  const { PaginationButton: Slot } = useComponents();
  return /* @__PURE__ */ (0, import_jsx_runtime44.jsx)(Slot, { ...props, isActive, label: props.children });
};

// src/components/Pagination/shared.tsx
var import_react21 = require("react");

// src/components/Pagination/EllipsisButton.tsx
var import_react20 = require("react");
var import_jsx_runtime45 = require("react/jsx-runtime");
var EllipsisButton = ({ gotoPage, inputProps, ...rest }) => {
  const [isInput, setIsInput] = (0, import_react20.useState)(false);
  const { PaginationEllipsisButton: Slot } = useComponents();
  const localize = useReactComponentsLocalize();
  return /* @__PURE__ */ (0, import_jsx_runtime45.jsx)(
    Slot,
    {
      className: rest.className,
      isInput,
      placeholder: localize("pagination.page_placeholder"),
      onToggle: () => setIsInput((v) => !v),
      onSubmitPage: (page) => {
        gotoPage(page);
        setIsInput(false);
      },
      buttonProps: rest,
      inputProps: isInput ? inputProps : void 0
    }
  );
};

// src/components/Pagination/shared.tsx
var import_jsx_runtime46 = require("react/jsx-runtime");
var usePaginationPages = (currentPage, totalPages, maxButtons, gotoPage, createPageButton, inputProps, buttonProps) => (0, import_react21.useMemo)(() => {
  if (totalPages === void 0) return [];
  const pages = [];
  for (let i = 0; i < totalPages; i++) {
    pages.push(createPageButton(i));
  }
  if (!maxButtons || totalPages <= maxButtons) {
    return pages;
  }
  const startEllipsis = /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(EllipsisButton, { gotoPage, inputProps, ...buttonProps }, "start-ellipsis");
  const endEllipsis = /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(EllipsisButton, { gotoPage, inputProps, ...buttonProps }, "end-ellipsis");
  const sideButtons = 2;
  const availableButtons = maxButtons - sideButtons;
  if (currentPage <= availableButtons / 2) {
    return [...pages.slice(0, availableButtons), endEllipsis, pages[totalPages - 1]];
  }
  if (currentPage >= totalPages - 1 - availableButtons / 2) {
    return [pages[0], startEllipsis, ...pages.slice(totalPages - availableButtons, totalPages)];
  }
  const startPage = currentPage - Math.floor((availableButtons - 1) / 2);
  const endPage = currentPage + Math.floor(availableButtons / 2);
  return [pages[0], startEllipsis, ...pages.slice(startPage, endPage - 1), endEllipsis, pages[totalPages - 1]];
}, [totalPages, maxButtons, gotoPage, inputProps, buttonProps, currentPage, createPageButton]);
var PaginationButtons = ({
  totalPages,
  totalItems,
  currentPage,
  goToPage,
  createPageButton,
  maxButtons = 10,
  buttonProps,
  inputProps,
  ...rest
}) => {
  const { PaginationContainer, PaginationSummary } = useComponents();
  const t = useReactComponentsLocalize();
  const pages = usePaginationPages(
    currentPage,
    totalPages,
    maxButtons ? Math.max(5, maxButtons) : false,
    (page) => {
      if (page >= 0 && totalPages && page < totalPages) {
        goToPage(page);
      }
    },
    createPageButton,
    inputProps,
    buttonProps
  );
  return /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
    PaginationContainer,
    {
      ...rest,
      items: /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)(import_jsx_runtime46.Fragment, { children: [
        totalPages === void 0 ? /* @__PURE__ */ (0, import_jsx_runtime46.jsxs)(import_jsx_runtime46.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
            Button,
            {
              onClick: () => goToPage(currentPage - 1),
              disabled: currentPage === 0,
              ...buttonProps,
              children: t("pagination.previous")
            },
            "previous"
          ),
          /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(Button, { onClick: () => goToPage(currentPage + 1), ...buttonProps, children: t("pagination.next") }, "next")
        ] }) : pages,
        Boolean(totalItems) ? /* @__PURE__ */ (0, import_jsx_runtime46.jsx)(
          PaginationSummary,
          {
            text: t("pagination.total_results", {
              count: totalItems
            })
          }
        ) : null
      ] })
    }
  );
};

// src/components/Pagination/Pagination.tsx
var import_jsx_runtime47 = require("react/jsx-runtime");
var Pagination = ({ maxButtons = 10, buttonProps, inputProps, pagination, ...rest }) => {
  const { page, setPage, initialPage } = usePagination();
  const totalPages = initialPage === 0 ? pagination.lastPage + 1 : pagination.lastPage;
  const currentPage = initialPage === 0 ? page - 1 : page;
  return /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(
    PaginationButtons,
    {
      goToPage: (nextPage) => setPage(nextPage),
      createPageButton: (i) => {
        const pageIndex = initialPage === 0 ? i : i + initialPage;
        return /* @__PURE__ */ (0, import_jsx_runtime47.jsx)(Button, { onClick: () => setPage(pageIndex), isActive: currentPage === pageIndex, ...buttonProps, children: i + 1 }, i);
      },
      currentPage,
      totalPages,
      totalItems: pagination.totalItems,
      maxButtons,
      buttonProps,
      inputProps,
      ...rest
    }
  );
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ActionCancel,
  ActionContinue,
  ActionEnd,
  ActionPause,
  ActionsContext,
  ActionsProvider,
  Balance,
  ClientProvider,
  ComponentsProvider,
  ConfirmActionModal,
  ConfirmModal,
  ConfirmProvider,
  Election,
  ElectionActions,
  ElectionContext,
  ElectionDescription,
  ElectionHeader,
  ElectionProvider,
  ElectionQuestion,
  ElectionQuestions,
  ElectionQuestionsForm,
  ElectionResults,
  ElectionSchedule,
  ElectionStatusBadge,
  ElectionTitle,
  Envelope,
  HR,
  OrganizationAvatar,
  OrganizationContext,
  OrganizationDescription,
  OrganizationHeader,
  OrganizationImage,
  OrganizationName,
  OrganizationProvider,
  Pagination,
  PaginationProvider,
  QuestionChoice,
  QuestionTip,
  QuestionsConfirmation,
  QuestionsEmpty,
  QuestionsError,
  QuestionsFormProvider,
  QuestionsTypeBadge,
  SpreadsheetAccess,
  VoteButton,
  VoteWeight,
  Voted,
  composeComponents,
  defineComponent,
  enforceHexPrefix,
  environment,
  errorToString,
  fetchSignInfo,
  getQuestionChoiceMeta,
  hasExtendedChoiceMeta,
  normalizeText,
  queryKeys,
  reactComponentsDefaultLanguage,
  reactComponentsNamespace,
  reactComponentsResources,
  results,
  useActions,
  useClient,
  useComponents,
  useConfirm,
  useElection,
  useLocalize,
  useOrganization,
  usePagination,
  useQuestionsForm,
  useReactComponentsLocalize,
  vote,
  walletFromRow
});
